# Daily Plan — 2026-06-22 (first run of the epic-approval mechanism)

**Produced by:** CoS (manual run of `ros-daily-plan`) · **Mechanism:** [daily-execution-mechanism.md](../../00_System/daily-execution-mechanism.md)
**For:** Guy's epic-level approval. Tier-A smalls run regardless; Tier-C needs an individual yes.

## Prod status
🟢 **Up.** `arborparentingapp.com` → HTTP 200, serving the real Firebase/Vite app. ⚠️ `/healthz` 404s on the public domain (no external uptime endpoint — OPS-A5).

## Reconciliation (last few days)
- **Shipped / live:** the app + brand domain + billing rails (RevenueCat) are live on prod (`origin/main` @ `8789423`).
- **Staged, awaiting a gate:** `REL-ARBOR-001` — the incremental release pipeline (`rel/arbor/001`, +1) + the green-gated council fixes (`review/rel-arbor-001-council`, +6, 661 tests pass). **Not merged** — correctly gated on the prod-promote setup (below).
- **Unmerged work to triage:** `claude/hero-arcade` (+12), `claude/elevation-wave-1` (+4), `claude/marketing-arbor-il-he` (+2), `claude/billing-durability` (+1), `claude/d2-comic-css` (+1), `codex/arbor-v2-architecture-foundation` (+1). Status unknown until triaged → Epic 2.
- **At risk of being dropped:** nothing on disk (all ROS work committed `2952184`). The unmerged Arbor branches are the only "is this finished work that never shipped?" question.

## Today's proposed EPICS (Tier-B — your approval)
| # | Epic | Items / owners | riskClass | Tier-C gate inside |
| :-: | :-- | :-- | :-- | :-- |
| **1** | **Unlock shipping (keystone)** | Set GitHub `production` Required Reviewer + WIF → merge + promote `REL-ARBOR-001` (pipeline + council fixes reach prod). Owner: `ros-release-lead` + Guy. | gated | **YES** — production-env reviewer, WIF, prod-promote (all Guy) |
| **2** | **Triage & queue the unmerged branches** | Assess the 6 branches above; decide what's real green work to ride the next train; mark the rest superseded. Owner: `ros-release-lead` + `arbor-orchestrator`. Autonomous analysis; shipping rides Epic 1's pipeline. | safe | none (analysis); shipping = Epic 1 |
| **3** | **Backlog hygiene + parent signal** | Promote the verified reality-check findings into `AP-`/`AM-` (kill feeder duplication); kick off `arbor-ux` voice-of-parent (the deepest gap — roadmap is agents-grading-agents). Owner: `arbor-pm` + `arbor-ux`. | safe | none |

## Tier-A smalls running autonomously today (FYI — no approval)
- Title/meta copy: `<title>` "Parent Development Support" → brand-spine-compliant (small copy fix).
- `/healthz` public hosting rewrite (OPS-A5 — gives an external uptime endpoint).
- Stage the small CIL conversion fixes green (JITAI i18n leak, dead footer links) for the next train.
> Note: all Tier-A *shipping* is blocked behind Epic 1 until the pipeline is live — these stage green now, ship the moment Epic 1 lands.

## Tier-C decisions needing Guy (the unlock)
1. **GitHub `production` env → Required Reviewer = Guy** (makes promote pause for you, not auto-run).
2. **WIF** (`GCP_WIF_PROVIDER` + `GCP_DEPLOY_SA`, retire `GCP_SA_KEY`).
3. **One prod-promote sign-off** → ships `REL-ARBOR-001` (OPS pipeline + council fixes) via canary.

---

# Execution log — 2026-06-22 (epics 1–3 approved, "do it all")

## Epic 2 — Branch triage (DONE, evidence-based)
| Branch | ahead/behind main | Verdict | Why |
| :-- | :-- | :-- | :-- |
| `claude/elevation-wave-1` | 4 / **0** | **→ next train (priority)** | green + current; design-consistency (CTA→`--gradient-cta` token, 18 files) + shareable Story-of-Child/pride cards — directly fixes the "CSS-inconsistency" + viral-loop findings |
| `claude/billing-durability` | 1 / 26 | **→ next train (gated: billing)** | RC webhook durability + `/webhooks` hosting route — hardens the **live** billing rails; small (2 files) |
| `claude/d2-comic-css` | 1 / 26 | **→ next train (after rebase)** | restores comic-book CSS dropped from main ("cards rendered flat") — the "free-tier look" finding; small (1 file +64) |
| `claude/hero-arcade` | 12 / **66** | **rebase + re-triage** | Academy comic-world + AI-Studio image-gen; built on stale base, parts likely already on main (image-gen fix) — do not auto-ship |
| `claude/marketing-arbor-il-he` | 2 / 26 | **→ `AM-`, reconcile** | IL/HE landing rebuild; marketing-owned; reconcile vs the live landing before shipping |
| `codex/arbor-v2-architecture-foundation` | 1 / **239** | **SUPERSEDED → archive** | May-30 "Arbor v2" scaffold experiment, 239 behind the live app; abandoned |

→ The 3 "next train" branches (elevation-wave-1, billing-durability, d2-comic-css) become **REL-ARBOR-002**, queued behind REL-ARBOR-001's pipeline going live (Epic 1).

## Epic 3 — Backlog hygiene + parent signal (✅ DONE, multi-agent, verified)
- **`arbor-pm`** promoted 8 verified findings → `AP-014…AP-021` on the product backlog (each with `from:` back-ref); marked 5 CIL feeders `promoted →`/`dropped`; **dropped the false `post-checkout` finding** (main already handles `?billing=success`). **CoS-verified:** all 8 AP- present, feeders marked, **zero double-tracking**. Highest-value new items: AP-016 (mobile-hero, 23.75) + AP-021 (wire /healthz+smoke+canary, 23.75) = REL-ARBOR-001/002 fodder.
- **`arbor-ux`** stood up `mesh/teams/voice-of-parent.md` — the deepest gap. **CoS-verified honest:** signal inventory all-Zero, no fabricated parent data. In-app feedback prompt = Tier-A (ships); interview budget **~₪750 (Level 4)** + outbound recruit = gated.
- **Loop closed (REPORT step):** prod up · nothing dropped · REL-ARBOR-002 queued · backlog de-duplicated · parent-signal function exists.

## Epic 1 — Unlock shipping (RUNBOOK — your 2 one-time setups, then I ship)
Both branches are pushed (`rel/arbor/001` +1, `review/rel-arbor-001-council` +6). To go live:
1. **GitHub → repo `guyrubin/PPPPtherapy-` → Settings → Environments → `production` → add Required reviewer = you.** (Makes the pipeline's `promote` job pause for your approval instead of auto-promoting.)
2. **WIF:** set repo Variables `GCP_WIF_PROVIDER` + `GCP_DEPLOY_SA`, then delete the `GCP_SA_KEY` secret + the IAM key. (Retires the long-lived prod key — see `docs/ops/wif-migration.md` in the Arbor repo.)
3. Tell me "go" → I open the PRs, merge `rel/arbor/001` then `review/rel-arbor-001-council` to main → CI green-gates → candidate deploys + smokes → **you click approve on the `promote` job** → 100%. Then REL-ARBOR-002 (the 3 triaged branches) follows the same path.
> I did **not** merge: until step 1 is set, a merge to main would auto-promote ungated. That's the one hard line.
