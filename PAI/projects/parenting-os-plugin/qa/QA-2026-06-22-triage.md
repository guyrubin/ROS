# QA Triage — 2026-06-22 (Hebrew QA pass)

**Source:** founder QA session (Google Doc, HE). **Owner session:** UX/UI/Feature Backlog.
**Routing:** bugs + small UI/feature = no-approval, built green on `claude/elevation-wave-1` → shipped THROUGH the `ros-release` train (canary+smoke → Guy promotes). Bigger UX = epics (Guy approves at epic level). Backend items → handed to the backend/other session.

## Bugs + small UI/feature (no-approval — building now)

| ID | Area | Finding (HE→EN) | Fix | Class |
|---|---|---|---|---|
| QA-1 | Onboarding | Can't enter age in months — only a slider; half a month is critical | B0 added a months slider (0–11) for under-3; ADD an optional exact **date-of-birth** input that takes precedence (most precise) | small feat |
| QA-2 | Onboarding | Missing **boy/girl** | Add `gender?` to ChildProfile + a boy/girl toggle (EN+HE), non-blocking | small feat |
| QA-3 | Onboarding | Name input caret doesn't auto-advance — must click (RTL) | `dir="auto"` on the free-text inputs so HE typing advances | bug |
| QA-4 | Onboarding/Profile | Added child (Lenny, age 1) but activities didn't update — stayed on prior child (Dylan) | Set new child active on add; make daily-play/activity selection read the **active child's age reactively** | bug |
| QA-5 | My Child | Story tab should be **last, not first** | Reorder the My Child tabs | small UI |
| QA-6 | Today | "Log a moment" button too large; move the modal **X to the inline-start (left in RTL)** | Resize button; move close affordance | small UI |
| QA-7 | Auth | Login screen in **English** — IL audience should see Hebrew | Default UI lang to HE for IL / detect; fully i18n the LoginScreen | small UI |

## Backend — handed to the backend/other session (not this lane)
| ID | Finding | Note |
|---|---|---|
| QA-8 | "Request access" email never arrives | Backend (email/auth) |
| QA-9 | Error when **saving a memory** | Investigate client→server; likely server |

## Epics (Guy approves at epic level)

- **EPIC-QA-A — Today page IA/visibility.** "Content is good but the layout/visibility isn't clear." Restructure Today's information hierarchy so the spine reads clearly (arbor-ux owns the flow + states).
- **EPIC-QA-B — My Child clarity.** "Add a moment" categories aren't age-appropriate; the memory area's **purpose + data source** is unclear. Redesign My Child IA + age-aware capture categories.
- **EPIC-QA-C — Ask Arbor memory-review value.** The memory-review area's added value is unclear. Define + show the value (what the parent gets from the longitudinal record).

> The age-appropriateness complaints (QA-1/QA-4 + EPIC-QA-B categories) all trace to the same root the elevation plan named: the app was tuned for the ~5yo and the under-3 / over-7 bands were thin. B0 (months-precise age) is the keystone; QA-4 (age propagation) makes it actually drive the experience.
