# Advisory score — DEM-006 (dispatched intake row)

> Dispatched by `arbor-advisor` for the Product Council to fold into `PRODUCT-COUNCIL.md` § "Council Intake — 2026-06-21 (advisory scores)". Written here, not into the canonical table, because that table was under concurrent write during this review (repeated stale-read collisions). Council: append the row below in the DEM block.

| ID | Title | Stream | riskClass | Priority | Clinical | Advisor | Owner pod | Note |
|----|-------|--------|-----------|----------|----------|---------|-----------|------|
| DEM-006 | Family-Level Multi-Child Dashboard — Cross-Sibling DevScore Glance | demand | gated | n/a | needs board pass | tension | arbor-growth | **Tension — right substance (real moat), wrong frame (retention/upsell + an implicit sibling-comparison glance).** Profiles are stored but the UI renders only `activeChild` and `ProfileSwitcher` is name+age only, so a family-level view is real value and is genuinely Arbor-defensible (each child's longitudinal DevScore is a signal no single-vertical competitor can generate). Scores positive on **order-from-chaos** (one calm place to hold a multi-child household instead of context-switching) and **truth** (more of the record made visible). But three lenses tilt wrong as written: (1) **meaning→engagement** — the rationale leads with "highest-retention cohort" + "natural Family plan upsell" + matching Lingokids/Khan; a retention/monetization frame, the pattern the rubric down-weights. (2) **truth→false-signal & competence→dependence** — a side-by-side cross-sibling DevScore *glance* invites parents to read DevScore as a comparative scoreboard and rank their children, exactly the "ability verdict" the dev-score's non-diagnostic stance (see CLI-02) exists to prevent; comparing siblings on a number pathologizes normal variation and trains reliance on the app's number over the parent's own read of each child. (3) **responsibility→comfort** — a glance optimized to *reassure* ("all my kids are fine at a glance") soothes rather than nudging agency. **Reframe — make it a per-child orientation view, not a comparison scoreboard:** (1) each child gets their *own* card answering "where is THIS child in THEIR development + the one thing to nurture next," each read against that child's own age-appropriate set, never against a sibling; render so two children's numbers are **not visually rankable** side by side — no shared leaderboard axis, no sort-by-score. (2) Every card ends in **one parent-owned next step per child** (observe X / a question to ask / when to involve a provider), not an upsell or a re-engagement. (3) Reframe the Family-plan tie as "hold each child's record in one place," not "compare your kids"; success metric = a parent acted on a child's next step, NOT opens/DAU/upsell conversions. Built that way it climbs to aligned. Correctly `gated`: aggregated child-data across multiple records + a Plus/Family billing surface → entitlement gate wired before UI, never auto-built, human decision. `touchesClinical:false` is understated — because each card surfaces a DevScore read, `arbor-clinical-lead`/`-peds` should confirm the non-diagnostic, per-child-windowed framing (pairs with CLI-02) before build-ready; flag needs-board-pass on the copy. Firewall: card copy carries no adviser/thinker identity, "developmentally informed," never "clinically validated." reach 3 / impact 4 / confidence 0.8 reasonable. |
