# Product Council — the one intake

**Version:** 1.0 · **Created:** 2026-06-21
**Owner:** PAI (product). **Mandate:** turn five input streams into **one scored `PRODUCT-BACKLOG.md`** the Orchestrator executes. Decides *what* gets built and *in what order*; never builds, merges, or deploys.

The council is how a real product company decides. It convenes the [Head of Product](teams/product.md), the [Advisory Board](teams/advisory.md), the [Marketing mesh](marketing/MESH.md), and the [CIL](improvement/CIL.md), reconciles their inputs, applies the clinical gate, scores, and hands a ranked, build-ready queue to [`arbor-orchestrator`](ORCHESTRATOR.md).

---

## 1. The five input streams

| Stream | Source agent | Brings | Tag |
| :-- | :-- | :-- | :-- |
| **Product** | `arbor-product` | Discovery briefs + PRDs + a success metric per candidate; the voice-of-parent (forward user research) | `product` |
| **Advisory** | `arbor-advisor` | Worth-building rubric scores + reframes (competence/order/responsibility/truth/meaning) | `philosophy` |
| **Clinical** | `arbor-clinical-lead` | Clinical requirements (what we *should* build to be sound) + the soundness/claim gate on everything else | `clinical` |
| **Marketing** | `arbor-marketing-lead` | Feature-requests from competitor gaps + demand signal (the funnel hands these over per its DoD) | `demand` |
| **Improvement** | `arbor-evaluator` (CIL) | Verified findings + themes from the live-app critic panel | `cil` |

> PAI roadmap intent and CoS portfolio constraints sit **above** the council and set its priorities; founder ([FOUNDER]) items enter directly at top priority.

## 2. The council loop (`/arbor-product-council`, weekly)

```
 SENSE ─────────► REVIEW ──────────► SCORE ──────────► WRITE ──────────► HANDOFF
 gather the 4    advisor rubric +    rank by the      candidate queue   top safe, build-ready
 streams         clinical gate       formula below    in PRODUCT-       items flagged for the
 (parallel)      on each candidate   (top ~12)        BACKLOG.md         Orchestrator's next wave
```

- **SENSE** — pull the five streams (the product + marketing + CIL backlogs already exist on disk; advisory + clinical are dispatched live).
- **REVIEW** — `arbor-advisor` scores each candidate on the rubric; `arbor-clinical-lead` runs the clinical gate on anything touching development/behavior/speech/health/claims.
- **SCORE** — compute priority; dedupe across streams (one root cause, not ten symptoms).
- **WRITE** — append a dated **`## Council Intake — <date>`** candidate block to `PRODUCT-BACKLOG.md`. The council **proposes**; it does not rewrite the canonical body. A human or the Orchestrator promotes candidates into the live tracks.
- **HANDOFF** — flag the top `safe` + `build-ready` items for the Orchestrator; surface `gated` and `[FOUNDER]` items as an explicit decision list.

## 3. Scoring

```
priority = (reach × impact × confidence × strategic_fit) ÷ effort
```
- **strategic_fit** folds in the advisor rubric (aligned > tension > off-rubric) and PAI roadmap intent — a high-engagement, off-rubric idea is *down-weighted on purpose*.
- **confidence** drops for thin demand signal or contested clinical evidence.
- Ties break toward the moat (longitudinal child record) and toward retention (the known constraint).

## 4. riskClass + the clinical gate (binding)

Every candidate carries a `riskClass`:
- **`safe`** — no claim, no child-data/billing/cost/consent surface. Auto-eligible for a build wave.
- **`gated`** — carries a developmental/medical/effect-size claim, or touches safety/consent/billing/cost/child-data, or surfaces the philosophy adviser's identity. **Never auto-built.** Filed for human decision.

**Clinical gate:** a candidate touching development/behavior/speech/health/claims **cannot be marked `build-ready`** until `arbor-clinical-lead` returns `soundness: pass` and `claims: none|substantiated`. A soundness VETO or an unsubstantiated claim holds the item, exactly like an `arbor-safety` veto.

## 5. Candidate block format (what the loop writes)

```md
## Council Intake — 2026-06-21
| ID | Title | Stream | riskClass | Priority | Clinical | Advisor | Owner pod | Note |
|----|-------|--------|-----------|----------|----------|---------|-----------|------|
| CI-01 | … | demand | safe | 8.4 | n/a | aligned | arbor-practice | … |
| CI-02 | … | clinical | gated | 7.1 | pass | aligned | arbor-growth | claim → needs Guy |
```

## Clinical GATE — 2026-06-21 — DEM-005 Live Expert Session Booking → soundness:concerns / claims:UNSUBSTANTIATED / riskClass:gated — HELD

**arbor-clinical-lead** + peds/slp/psych ran the binding gate on **DEM-005** (calendar + video + in-app payment, memory-packet pre-load; `consult/` + Appointments screen; touchesClinical:true). **Verdict: soundness:concerns / claims:UNSUBSTANTIATED / riskClass:gated → HELD** (not build-ready; identical to an arbor-safety veto).

- **Not a clinical VETO** — the underlying capability (route the parent OUT to a real human, pre-loaded with the child's own record) is clinically sound and rubric-positive: it strengthens the real-world provider relationship rather than substituting an app one. The board's concerns are about *framing and claims*, all fixable.
- **claims:UNSUBSTANTIATED (two):** (1) the candidate's own `claim`/rationale asserts the packet makes sessions **"richer than any cold telehealth call"** — a comparative *effect/quality claim about clinical encounter outcomes* with no evidence; there is no data that an Arbor memory packet improves session quality or outcomes. Downgrade to a *mechanism* statement ("the provider you choose can see a record you share," never a quality/outcome claim). (2) the rationale's **"expert"** label is itself a latent clinician-endorsement claim the moment booking copy implies Arbor *provides/employs/endorses* licensed care (CHARTER §3 p11 — marketing+safety veto). Neither may ship as written.
- **soundness:concerns (the binding required_fixes):** the deeper clinical-soundness risk is **care-substitution + dependence**: a metric of *"packet-share AND book = 15% of exports"* optimizes consults-booked, which trains "the app books my parenting out" over the parent's own judgment + a durable real provider relationship — and a booking marketplace that *replaces* (vs. routes to) the family's existing pediatrician/SLP cuts against AAP medical-home continuity and ASHA scope (a one-off marketplace session is not a substitute for the child's ongoing clinician).
- **Lens splits:** **peds concerns/blocking** — must default to inviting the parent's *existing* provider before any marketplace, and must never present a booked expert as the child's medical home or as Arbor's clinician; medical-home continuity (AAP). **slp pass/conditional** — fine for an SLP consult *if* framed as "an independent professional you choose," with the packet scoped to relevant practice data, never "Arbor's speech expert." **psych concerns/blocking on framing** — a consult-rebooking loop is a reassurance/dependence channel; the loop must reduce need over time ("do you still need this?"), success = a concrete plan the parent owns / fewer repeat consults, NOT consults-booked or rebooking rate.
- **required_fixes (binding before build-ready):** (1) **drop the "richer than cold telehealth" claim** — replace with a non-comparative mechanism description; no session-quality/outcome claim. (2) **Provider-identity firewall (hard):** "an independent professional you choose," explicit in-copy "Arbor does not provide medical care; these professionals are not employed, supervised, or endorsed by Arbor," no licensure implied by Arbor's brand; the word "expert"/"clinical" never reads as an Arbor endorsement (CHARTER §3 p11). (3) **Default to invite-your-own-provider** before any marketplace; the marketplace is the fallback, not the front door (AAP medical-home continuity). (4) **Packet = the product, booking = the wrapper:** per-session, scoped, time-boxed, revocable consent framed as the parent granting access they own (a single checkbox is not sufficient consent); success metric = *session produced a parent-owned plan / fewer repeat consults over time*, NOT consults-booked / rebooking / 15%-of-exports. (5) **Graduation stance** over a subscription-to-consults loop. (6) Firewall §0/§3 p11 — all booking + packet copy "developmentally informed," never "clinically validated"/"clinician-reviewed," and carries no adviser/thinker identity.
- **Still gated by procedure regardless:** the two hardest gates in the product co-exist here — **billing** (Stripe session payment, Level 4) and the **deepest third-party child-data egress** (full memory packet to an external party, COPPA/GDPR) — both require Guy + `arbor-safety` sign-off independent of the clinical hold.
- **cited_basis:** AAP medical-home / care-continuity (a one-off marketplace consult does not replace the child's ongoing clinician); ASHA scope-of-practice (a chosen SLP, not "Arbor's expert"; software does not provide or endorse care); CDC LTSAE 2022 (surveillance routes the parent to *their* provider conversation). CHARTER §3 principle 11 (no clinician-endorsement claim).
- **Verdict:** soundness:concerns / claims:UNSUBSTANTIATED / riskClass:gated → **HELD**. Cannot be build-ready until the "richer-than-telehealth" claim is dropped, the provider-identity firewall + invite-your-own-provider default + packet-not-booking metric are in, and `arbor-safety` clears billing + child-data egress. Routed through `arbor-orchestrator` as blocked-for-build (identical to an arbor-safety veto).

## Clinical CLAIMS GATE — 2026-06-23 — M3 store-listing metadata (EN + HE) → soundness:concerns / claims:UNSUBSTANTIATED(3) / riskClass:gated → HELD

**arbor-clinical-lead** + peds/slp/psych ran the binding claims gate on the **M3 store-listing metadata pack** (EN + HE; `execution/mobile-store/M3-listing-metadata.md`) for public Apple/Google submission. **Verdict: soundness:concerns / claims:UNSUBSTANTIATED / riskClass:gated → HELD.** Not submission-ready. The pack's own claim-audit (line 422-428) over-states the result: it asserts "no improves/boosts language anywhere," but the **"predict[s] the whole day"** Rhythm copy is a behavioral-outcome/accuracy claim it missed, present in BOTH stores and BOTH languages. These lines cannot ship in a public listing.

**Not a clinical VETO** — the underlying product is sound and the moat copy ("knows your kid," "the record") is claim-free and approvable. The board's holds are on three over-claiming surfaces, all fixable with non-comparative, mechanism-only rewrites that keep the marketing punch.

**claims:UNSUBSTANTIATED (3) — must change before submit (severity reject):**
1. **Rhythm "predicts your child's whole day" (effect/accuracy claim).** EN Apple L114 "predict the whole day's friction and calm windows"; EN Play L176 "predicts your child's whole day"; HE Apple L327 "מנבא חלונות רוגע ומתח לאורך כל היום"; HE Play L375 "מנבאה חלונות רוגע ומתח ליום שלם." A behavioral-prediction-accuracy claim with no validation — implies a measurable forecast capability. **Peds + psych concur:** a definite "predicts" trains parent reliance on the app's forecast over their own attunement and asserts an unproven behavioral effect. **Safe rewrite (EN):** "helps you *anticipate* likely calm and friction windows from your child's *own* logged patterns" / "reflects your child's *own* rhythm back to you — likely calm and friction windows from what you've logged." **HE:** "עוזרת לכם לזהות מראש חלונות רוגע ומתח, לפי הדפוסים שתיעדתם" (anticipate-from-your-own-logs, not "predicts the whole day"). Drop "the whole day / ליום שלם" absolute-scope language.
2. **HE Promotional Text "כבר יודעת עליו הכל" = "already knows EVERYTHING about him" (absolute-knowledge over-claim) + adds "שפה" (language tracking) not in the EN promo.** HE L274. The EN promo (L55) is correctly scoped ("already knows who you're asking about"); the HE transcreation inflated it to an absolute and silently added language-tracking to the domain list. **Safe rewrite:** "...וכל שאלה שתשאלו כבר יודעת על מי מדובר" (knows *who* you're asking about) — match the EN scope; drop "הכל." Only list "שפה" as a tracked domain if Practice Studio language-data actually writes to the record AND FR-10 is verified.
3. **Practice Studio "speech, language, and social-emotional learning" + "sessions feed back into the record so the coach has something real to reference" — implied developmental-intervention loop, ungated.** EN Apple L120, EN Play L182, HE L333 ("משחקי שפה ולמידה חברתית-רגשית... הפגישות חוזרות לתיעוד"). Naming the game domains is fine as *content description*; the hazard is the "feeds back to the coach" loop reading as an *intervention that works*. The pack itself flags FR-10 unverified (L436). **Hold:** until FR-10 is verified live, strip the feedback-loop clause in all four places and use the pack's own fallback — "child-facing speech, language, and social-emotional games." Never imply therapeutic effect or skill improvement. (HE note: L333 drops "speech/דיבור" vs EN and has a typo "מה לאמר"→"מה לומר"; reconcile in the rewrite.)

**soundness:concerns (binding required_fixes):**
- (a) **Replace every "predict" with "anticipate / reflect back / help you see"** + scope to "your child's *own* logged patterns" (mechanism, not forecast accuracy) — EN + HE, Apple + Play.
- (b) **HE transcreation parity:** the Hebrew inflated two claims past the English (absolute "knows everything," added language-tracking). Hold the HE to the *same* claim ceiling as the EN; transcreation may sharpen voice, never escalate a claim.
- (c) **Honor the FR-10 deployment gate before submit:** Practice Studio feedback-loop clause is removed (both stores, both languages) unless the live loop is verified.
- (d) **Apple age-descriptor consistency (M5):** M5 correctly says do NOT select "Medical/Treatment Info"; the listing must stay non-diagnostic so the 4+ rating holds. The corrected copy keeps this true.
- (e) **Firewall §0/§3 p11 (PASS, hold it):** no "clinically validated / clinician-reviewed" anywhere, no adviser/thinker identity, "non-diagnostic" stated (L118/L180), Care = packet/warm-handoff only (no marketplace/booking) — all clean; keep it that way through the rewrite.

**Lens splits:** **peds — blocking on #1** (a behavioral-prediction-accuracy claim is exactly the effect-size class the board vetoes); **slp — blocking on #3** (Practice Studio must not imply a speech/language *intervention effect*; "games," never "therapy/feeds the coach who fixes it"); **psych — concerns on #1+#2** (a definite "predicts your child" + "knows everything" tilts parent toward app-reliance over their own read; reflect-back framing is attachment-safe and competence-building).

**cited_basis:** CDC "Learn the Signs. Act Early." (2022) — surveillance + parent-to-provider conversation, no app verdict; AAP developmental-surveillance — observe/route, never assert outcome; ASHA scope-of-practice — speech/language progress signals belong to a qualified professional, software games do not claim intervention effect; CHARTER §3 principle 11 + advisory.md §0 (no clinician-endorsement, no "clinically validated").

**Verdict:** soundness:concerns / claims:UNSUBSTANTIATED(3) / riskClass:gated → **HELD.** Cannot be submitted until the three claims are rewritten (both languages, both stores) and FR-10 honored. Routed through `arbor-orchestrator` as blocked-for-submission, identical to an arbor-safety veto. Everything else in the pack (app name, subtitle, short desc, keywords, moat/privacy copy, categories) is **clinically clear**.

## Clinical GATE — 2026-06-23 — CI-24 FeelingsLab Personalized Layer (build-ready design) → soundness:concerns / claims:none / riskClass:gated → BUILD-READY-NARROWED (psych+safety co-sign + verbatim strings pending)

**arbor-clinical-lead** + psych (lead) / peds / slp ran the binding gate on the **CI-24 build-ready design** (`CI-24-FeelingsLab-Personalized-Layer`: child Check-In, parent Personalized Toolbox, `RegulationPatternView`, `EscalationNudge`). Ruled against the pre-set bar in `mesh/improvement/CLINICAL-GUARDRAILS-CI-22-23-24.md` §CI-24 — **not re-derived**. **Verdict: soundness:concerns (clearable) / claims:none / riskClass:gated → BUILD-READY-NARROWED.** The design **honors the gate's shape**: generic 6-emotion UI, no zones, no child intensity, timeline-not-chart, fire-once escalation, co-reg-outcome trending. It is buildable to the narrowed scope below the moment psych + safety co-sign the exact strings; it is **not** free-build until then.

**NO clinical VETO. No veto trigger present.** Confirmed clean against the §1 veto list: no per-child anxiety/emotion score or verdict, no intensity trend chart, no recurring escalation alert, no condition name, no Zones vocabulary, no kid-companion substitute, no inference of the child's internal state. The personalization moat is attachment-positive precisely because it routes the parent's OWN logged "this worked" strings back through the parent — it never has the app classify or soothe the child.

**Narrowed scope (build ONLY this — the gate clears this and no more):**
- **Child Check-In:** six generic emotions (happy/sad/angry/scared/confused/silly), icon/AAC primary path, ≥56px targets, no intensity selector, no zone color, one `emotion-checkin` PracticeEvent per session. Conforms.
- **Personalized Toolbox (parent-facing):** parent's OWN `BehaviorLog.response` strings where `resolved=true`, deduped, ranked by occurrence count. This is a **mechanism description (what you logged as working)**, NOT a child score — clears. Logged-count is a tally of the PARENT's actions, never a child metric.
- **RegulationPatternView (parent-facing):** a **list timeline**, explicitly not a chart; trends parent-logged **co-regulation outcomes**, never the child's emotion-word count/range/intensity. Conforms to the §2 slp emotion-vocab-leak rule.
- **EscalationNudge:** fires ONCE per pattern-episode, two-conjoint AND threshold (intensity AND duration), ≥90d cool-down, never shown to child, dismissed-state persisted. Conforms to §3.

**required_fixes (binding before build-ready → soundness:pass):**
1. **EscalationNudge copy = the §3 string VERBATIM, no paraphrase.** The design correctly defers ("exact copy specified in clinical guardrails") — the build pod must paste the §3 string exactly: *"Over the past few weeks you've logged [child] feeling [scared/sad] strongly and often. That can be completely normal — and it can be worth bringing up with your pediatrician or a child mental-health professional next time you talk to them. You know [child] best."* No edit without a board re-pass. The string is also the ONLY place "strongly and often" may appear; it describes the logged pattern, never a verdict.
2. **Set the still-open child-facing strings now (clinical-locked):** Check-In header = *"How are you feeling right now, [name]?"* (verbatim from §1). Post-pick MascotSay (warm, non-evaluative, NO praise-for-feeling, NO "good/bad" valence) = *"Thanks for telling me."* Parent-transition hint = *"Now for the game"* is fine (neutral). Toolbox header = *"Strategies that have worked before with [name]"* (verbatim §1). Timeline label = *"What [name] named, and what helped"* (verbatim §1). Every strategy card carries *"Co-regulation comes first"* (§1) — the parent does it WITH the child; the app never soothes the child directly. These are clinical-locked; no free-write substitution.
3. **Intensity is internal-only and never rendered.** The design references a "high-end internal-only intensity capture" that feeds only the fire-once rule. Binding build-time assertion: NO intensity value reaches any child- or parent-facing surface, NO intensity-aggregation code path, NO emotion-word-count aggregation. The pattern view has no chart primitive. (peds: an unlabeled intensity-over-time line IS an anxiety-trend proxy → medical-device-adjacent, EU MDR Rule 11 — dropped entirely.)
4. **Output-floor (precondition):** the strategy text is parent-authored (logged response strings) so is low-risk, BUT any model-authored pattern-view summary, ranking blurb, or "where to lean in" string MUST pass `screenModelOutput` (`safety/outputScreen.ts`) with the `CONDITIONS` regex covering `anxiety`, `anxiety disorder`, `depression`, `depressed`, plus a passing `safety/outputScreen.test.ts` case (synthetic insight with a label-leak caught + safe fallback). If the build ships zero model-authored strings on these surfaces, document that and the floor is satisfied by construction — but the parent's own free-typed response strings displayed back are NOT model output and need no screen (they are the parent's words, not Arbor's verdict).
5. **Escalation guard test:** fire-once + ≥90d cool-down implemented with a test asserting a second pattern-episode INSIDE the cool-down does NOT re-fire. No badge/counter/"still elevated" re-prompt.
6. **arbor-safety COPPA co-sign (hard, parallel gate):** three new child-data write paths — the `emotion-checkin` PracticeEvent, the `feelingsEscalation` subcollection (dismissed state), and the "Mark tried today" calm-event write. Co-sign required before build-ready, identical to the §CI-24 co-sign requirement.
7. **slp AAC floor:** the icon/visual selection path is the PRIMARY input (no text required) so a minimally-verbal / late-talking child is not excluded or implicitly flagged. The design honors this — hold it.

**Lens splits:** **psych (lead) — concerns, clearable:** the design is attachment-safe BY CONSTRUCTION (routes through the parent, hands back the parent's own working strategies); the only residual risks are (a) the MascotSay line drifting into evaluating the child's feeling ("great job feeling happy!") — locked to non-evaluative in fix #2 — and (b) the timeline reading as a verdict if a chart ever appears — locked out by fix #3. **peds — pass, conditional on fix #3:** dropping the anxiety surface and keeping intensity internal-only is the correct AAP-surveillance posture; an intensity trend would be a per-child anxiety inference (medical-device-adjacent). **slp — pass, conditional on fixes #2/#7:** AAC-primary input + trending co-reg outcomes (not emotion-word count) keeps this out of expressive-language-assessment territory.

**cited_basis:** CDC "Learn the Signs. Act Early." (2022) social-emotional milestones + surveillance-routes-to-provider posture; AAP developmental surveillance (observe/route, never label); affect-labeling / "name-it-to-tame-it" co-regulation literature (mechanism, back-end only, never cited in copy); EU MDR Rule 11 / FDA SaMD on per-child anxiety inference (the drop rationale); advisory.md §0 / CHARTER §3 p11 firewall ("developmentally informed," never "clinical/clinically validated").

**Verdict:** soundness:concerns / claims:none / riskClass:gated → **BUILD-READY-NARROWED.** Buildable to the narrowed scope above the moment (i) the 7 required_fixes are in the actual UI/strings, (ii) the escalation guard + output-floor are green and tested, and (iii) `arbor-clinical-psych` + `arbor-safety` co-sign the exact strings + the child-data write paths. Until then it stays GATED — not free-build. Routed through `arbor-orchestrator` as build-ready-narrowed-pending-cosign, identical to an arbor-safety conditional clear.

## Clinical GATE — 2026-06-23 — "100 Capability Blueprint" → soundness:pass-in-principle / NO VETO / 11 line-items gated-but-clearable, 2 re-touch a CEO-gated decision

**arbor-clinical-lead** + peds/slp/psych ran a **targeted** soundness+claims gate on the **100 Capability Blueprint** (8 domains, 100 capabilities; the firewall is baked into the data model as a typed `prohibitedDiagnosticClaims`/`notAllowedClaims` field per capability). Reuses the binding bar in `mesh/improvement/CLINICAL-GUARDRAILS-CI-22-23-24.md` + `mesh/improvement/BABY-CLINICAL-REQUIREMENTS-2026-06-22.md` (B3 swallow-safety + banned strings) — not re-derived. **This is the input that PASSES where Framework-v2 was vetoed:** v2 carried an assessment-engine / risk-map / instrument-mapping overreach; this blueprint is *deliberately diagnosis-free* (bans "autism detected"/"ADHD detected"/"risk score"; referral = soft safety only). That design choice is the difference between vetoed and clearable.

**1. Overall verdict — CLEARABLE IN PRINCIPLE, no VETO.** The safe-language enrichment model ("skills practiced," "patterns noticed," "consider discussing with a professional," "developmental enrichment," "parent-guided activity") holds Arbor **non-diagnostic, non-device, and 4+-rateable** — *provided* the per-item guardrails below are in the actual strings and the output-floor is enforced. The "best 15-min developmental activity today, NOT diagnose my child" north star is the correct, defensible center of gravity. **No clinical VETO issued.** Veto triggers remain live and unchanged (any per-child score/verdict/risk; condition name in any string; an intensity/frequency trend chart; a recurring escalation alert; a branded-program name; a kid-companion substitute; inference of the child's internal state; an exposure ladder that overrides a swallow-safety signal).

**2. Bless the smart primitive — `prohibitedDiagnosticClaims` as a typed first-class field = genuinely strong safety architecture.** It operationalizes the firewall *in the data model* (declarative, per-capability, reviewable, lintable) rather than leaving it to copy discipline — this is the right place for it and is a real improvement over a prose policy. **Recommendation (binding for build-ready):** the typed field must be ENFORCED, not merely declared — every AI-/model-authored activity/story/summary/insight string on any capability MUST pass `screenModelOutput` (`safety/outputScreen.ts`) at runtime, with the capability's own `prohibitedDiagnosticClaims` compiled into the `CONDITIONS` screen, plus a passing output-screen test (the CLI-06 precedent made load-bearing across the blueprint, not just `BehaviorsTab`). A declared-but-unenforced field is a comment, not a guard.

**3. Per-item flags (11 line-touching items) — allowed framing + the exact line / the vetoed version:**

| # | Item | Allowed framing | The line it must NOT cross (vetoed) | riskClass |
|---|---|---|---|---|
| #41 | Fear & Anxiety Stories | parent-mediated shared story rehearsing a feared *situation* + a coping phrase; child names fear / practices coping IN-the-moment | the **word "anxiety" in the feature/label name is a leak** (CI-24 dropped the anxiety surface) → **rename to the situation** ("Brave Stories" / "The Dark-Room Story"); no count/trend of "fears named over time" (= anxiety-trend proxy); no intensity scale to the child; **non-verbal/AAC/parent-reported coping must count** — verbal-only completion implicitly flags minimally-verbal children (SLP); never a kid-companion (parent in the loop) | **gated-clearable** |
| #45 | Meltdown Reflection | POST-EVENT, parent-facing reflection (event/trigger/need/next-strategy); AI strategy text mechanism-only | must **not fire during a live crisis** (co-regulation first); no `dysregulation`/`oppositional`/`ODD`/`defiant` label; AI "next strategy" passes the output-floor + no effect claim ("this will reduce his meltdowns"); **repeated meltdown logs must REUSE the CI-24 fire-once escalation, never grow a "meltdowns trending up" chart or recurring alert** | **gated-clearable** |
| #62 | Sensory Preferences | a parent **preference/comfort log** read back as "what helps [child] feel comfortable" | **the SPD-screen line:** no count/threshold/verdict over logged dislikes ("6 of 8 sensory signs → may indicate SPD"); banned: `Sensory Processing Disorder`/`SPD`/`sensory processing`/`over-/under-responsive`; **`sensory profile` and `sensory seeker` are label-leaks** (instrument-frame + trait-noun) → trend "what helps," never a trait. SPD is not a DSM-5 dx + AAP cautions against it; **no CDC anchor → descriptive log, zero interpretation** | **gated-clearable** |
| #67 | Night Waking Log | descriptive parent log ("when [child] woke, what you noticed"), child-as-own-baseline | **the sleep-disorder-read line:** no `sleep disorder`/`insomnia`; **no normative cutoff rendered as a threshold** ("most 2yos wake 0–1×; [child] is above range" reads diagnostic even without "disorder"). **Under-1 AAP safe-sleep guard is load-bearing** — must never surface bed-sharing/weighted-sack/positioner as "what helped"; infant waking is SIDS-protective, never framed as a problem to reduce (reuse B3) | **gated-clearable** |
| #69/#70 | Feeding Exploration / Texture Tolerance | non-scored **exposure ladder** for food *acceptance* ("tried, liked, wasn't ready yet") | **the ARFID line + the swallow-safety floor (SLP, the one near-physical-safety item):** banned `ARFID`/`avoidant-restrictive`/`feeding disorder`; AND a **swallow-safety interrupt must sit ABOVE the ladder** — coughing/gagging/choking, wet-gurgly voice, nasal regurg, breathing change while feeding, refusal-of-a-texture-class + poor weight gain = **STOP-and-route, never a rung** (aspiration risk). Also route, never "track": choking event, FTT/weight-loss, allergic reaction; under-12m = "talk to your pediatrician about what/when to introduce." Reuse B3 banned strings (`dysphagia`/`aspiration`/`oral-motor`) | **gated-clearable (interrupt is the condition)** |
| #72 | Toilet Readiness | **child-readiness** checklist ("notices body signals," "stays dry longer") — genuinely sound surveillance, the AAP child-readiness (not age) frame | no readiness **score/verdict** ("7/10, behind, should be trained by now"); **constipation / stool-withholding / painful BMs / regression = MEDICAL, route to provider** — not a readiness item that implies "push harder" (withholding is the #1 training-failure driver; a readiness frame there does harm); no `enuresis`/`encopresis` labels | **gated-clearable** |
| #91 | Parent Concern Intake | output = **reframe-to-goals + soft referral**; acknowledge → restate as an observable goal/situation → offer the (non-auto-firing) referral rail | never a verdict in *either* direction — no "sounds like autism/ADHD/anxiety," no false-reassurance "he's fine"; a speech concern yields goals + the **reused CI-23/CI-25 "worth raising with your pediatrician or an SLP" rail** (never invent a new string); free-text reframe MUST pass the output-floor; **the four SLP referral numbers must be source-verified LIVE before any rail copy ships** (CLI-05 discipline) | **gated-clearable** |
| #95 | Progress Notes (parent/therapist/AI summary) | AI summary = **parent-attributed, behavior-anchored, non-metricized recall** ("you noted [child] used more two-word phrases this week") | must not read as an expressive/receptive-language assessment: banned `improving articulation`/`speech is improving` (clinical term + banned effect-on-language verb), **word-count/vocab-size as a trended metric** (= implicit expressive-language metric, the CI-24 §2 leak applied to speech), `intelligibility`/`expressive`/`receptive`/`MLU`/`on-track`. Output-floor + lint. **The "therapist/AI export report" re-touches the clinician-export question → see cluster note** | **gated — RE-TOUCHES a held decision** |
| #97 | Professional Mode (session planner / observation checklist / **export report**) | positioned "educational/developmental support tool"; the **only sound near-term form of any export is a PARENT-OWNED export the family brings to their own provider** (DEM-005 / AP-036 / B3 stance) | a **clinician-facing "export report" / therapist console is the held, CEO-gated-at-12-months clinician surface** — not near-term clearable; "Professional Mode" must not imply Arbor *provides/employs/supervises* a clinician (firewall §3 p11); any third-party child-data egress is a separate Guy/`arbor-safety` consent gate | **gated — RE-TOUCHES a CEO-gated decision** |
| #98 | Referral Guidance | soft AAP-surveillance nudge as default ("a good thing to bring up at your next visit — you know [child] best") — **correct posture** | **a tiered escalation is required** — the soft "next visit" tone is **too weak** for true red flags (developmental regression / skill loss; no words by 16m / no 2-word by 24m; breathing/seizure/LOC, serious allergy, poisoning, choking; self-harm/abuse). Those must escalate firmly ("don't wait" / emergency services), never be diluted to the soft string — a single soft tone across all triggers is a false-reassurance failure | **gated-clearable** |
| #99 | Content Safety Layer (banned language + crisis copy + disclaimers) | the right place to **centralize** the firewall; #41/#45/#91/#95 should *consume* this, not reimplement | crisis-escalation copy **MUST reuse the existing path** (`escalation.ts` + the CI-24 fire-once surveillance nudge + the product-CLAUDE.md trigger list), **never a new bespoke crisis flow** (a fork drifts wording + bypasses the tested guard = a safety regression). Acute path = direct "emergency — contact emergency services / a crisis line now," no normalization-hedge; persistent-distress path = the exact CI-24 fire-once string; disclaimers firewall-clean ("developmentally informed," never "clinically validated"). No condition name / risk score / severity level in escalation copy | **gated-clearable** |

**4. Claims register (latent claims the blueprint could still tempt):**
| Claim it could tempt | Verdict |
|---|---|
| "developmental enrichment / parent-guided activity" (mechanism, no effect size) | **none** — clean, the model's safe vocabulary |
| "skills practiced / patterns noticed" (parent-observed, descriptive) | **none** — clean if it stays parent-attributed and never trends a child metric |
| any "improves/builds/boosts/trains/strengthens/reduces" on a child's EF/language/regulation/sleep/feeding | **VETO** (effect-size on a child capacity — the claim ceiling) |
| "feeds back into the record so the coach references it" → reads as an intervention that works (#95/#97) | **substantiable only as a mechanism**, never an outcome; ungated FR-10-style intervention-loop claim = **VETO** until verified |
| Professional Mode / "export report" implying Arbor provides/endorses clinical care | **VETO** (clinician-endorsement, firewall §3 p11) |
| a sensory/sleep/feeding log compared to a norm to flag a possible condition | **VETO** (per-child condition inference = MDR Rule 11 device-adjacent) |

**Net it out (cluster verdicts):**
- **SAFE to build (the enrichment core):** the 8-domain / 100-capability games/stories/routines/parent-guided activities with the safe-language model + the typed `prohibitedDiagnosticClaims` field — **soundness:pass-in-principle, claims:none, riskClass:safe** *once the field is enforced via `screenModelOutput`*. The smart primitive is blessed.
- **GATED-but-CLEARABLE with the guardrails (9 items):** #41, #45, #62, #67, #69/#70, #72, #91, #98, #99 — **soundness:concerns(clearable), claims:none|substantiable, riskClass:gated.** Each clears when the per-item guardrail above is in the *actual strings* + output-floor bound/tested + final board copy pass. #69/#70 is the one with a hard physical-safety floor: **without the swallow-safety interrupt I recommend-veto; with it, pass.**
- **RE-TOUCHES a held/CEO-gated decision (2 items):** #95 (AI summary export) and #97 (Professional Mode "export report" / therapist console) re-open the **clinician-export question that is CEO-gated at 12 months (DEM-005 / AP-036)**. The **only** sound near-term form is a **PARENT-OWNED export the family brings to their own provider** — a clinician-facing console/report is **HELD**, not near-term clearable, and any export carries a separate Guy/`arbor-safety` child-data-egress gate. Surface to Guy; do not auto-build.

**cited_basis:** CDC "Learn the Signs. Act Early." 2022 (Zubler et al., *Pediatrics* 2022 — surveillance, no app verdict; checklists stop at 5, no sensory/sleep/feeding-texture/toileting anchors); AAP surveillance-vs-screening (Lipkin/Macias 2020); AAP Safe Sleep (Moon et al., *Pediatrics* 2022); AAP toilet-training child-readiness (Bright Futures); AAP sensory-processing caution (2012, SPD not a stand-alone dx); ASHA Pediatric Feeding & Swallowing + Late Language Emergence + SLP Scope of Practice; ARFID = DSM-5 (label-leak by definition); EU MDR Rule 11 / FDA SaMD (per-child condition inference = device, Cognoa precedent); CHARTER §3 p11 + advisory.md §0 (firewall). Reuses CI-22/23/24 + BABY-CLINICAL-REQ B3 verbatim. **Honesty flag:** no CDC milestone anchor exists for sensory/sleep/feeding-texture/toileting — those items must stay descriptive logs with zero interpretive scoring, and the AAP policy currency (Safe Sleep 2022, sensory 2012, toilet-training) + the SLP referral numbers should be re-verified live by a licensed professional before strings ship (same discipline as the EF age-7 floor).

**Verdict:** soundness:**pass-in-principle / NO VETO**; the enrichment core is **SAFE**, 9 line-items are **gated-but-clearable** with the named guardrails, and **2 (#95/#97 export) re-touch the CEO-gated clinician-export decision and are HELD**. Routed to **arbor-pm** as targeted findings (no backlog file written by the board); the typed `prohibitedDiagnosticClaims` field must be enforced via `screenModelOutput` on any AI-authored string before any clearable item is build-ready.

## Clinical GATE — 2026-06-23 — CI-25 Neurodiversity-aware Observation Lens (joint attention + perspective-taking observed-behavior log) → soundness:concerns(clearable, social-communication cluster narrowed) / claims:none / riskClass:gated → BUILD-READY-NARROWED (NOT blocked)

**arbor-clinical-lead** + peds/slp/psych ran the BINDING gate on **CI-25** (a parent-register-only, dated descriptive observation LOG across four clusters — Attention & focus / Social communication / Sensory responses / Language & speech — Yes/Sometimes/Not-noticed + free-text per behavior-prompt; saved as dated entries; a date-grouped timeline; a parent-owned, deselectable "provider brief" export via serializePacket; an assessment-prep guidance page). **Reuses, does not re-derive:** the Assessment-Layer partial-VETO below (observation-vs-assessment line + the SRS construct-clone VETO of #4 Social Communication Screener + the M-CHAT hard rule); CLINICAL-GUARDRAILS-CI-22-23-24.md (firewall §0; condition-name + effect-verb bans; CI-24 §2 child-metric-chart leak; CI-22 "observations over time" frame + age-7 floor; output-floor precondition); CI-28 BUILD-READY-NARROWED + the non-auto-firing referral rail; Blueprint #91/#95/#97 + DEM-005/PHI-08 egress. **Verdict: BUILD-READY-NARROWED — buildable as a descriptive log that emits NOTHING about the child. NOT a clinical VETO.** The flagship problem (the highest-anxiety cohort needs a calm, longitudinal, provider-routing way to name what they observe) is real and lands on the blessed buildable side — BUT joint attention + perspective-taking are the most sensitive constructs in the product and two narrowings are non-negotiable.

**1. Why this CLEARS rather than blocks.** CI-25 **emits nothing about the child**: no score, no %, no risk-map, no verdict, no norm-cutoff, no aggregation, no intensity scale, no trend chart, no "X of Y watch-areas" summary, no condition inference. Yes/Sometimes/Not-noticed are **not summed** (spec: "No scores are computed. No answers are aggregated into a verdict"). The timeline renders the parent's OWN dated entries + free-text — a recall artifact (Blueprint #95 + CI-22 "observations over time"), not a child metric. Export is parent-owned, deselectable, L3-gated, header self-declares "Not a diagnostic document." Child sees nothing. This is the "descriptive, parent-owned log that produces NOTHING about the child" the Assessment-Layer gate blessed. **No VETO — provided the two narrowings hold.**

**2. NARROWING (i) — Social communication cluster: the prompts ARE the gate; joint attention + perspective-taking name SRS/M-CHAT constructs (slp + psych, binding).** Highest-risk surface. The board VETOED #4 Social Communication Screener because its items reproduced the SRS-2 construct and functioned as an unvalidated autism screen (M-CHAT-rule analogue). CI-25's "Social communication" cluster is the SAME construct space — joint attention (responds to name, shared gaze, shows/points to share) and perspective-taking (reads others' feelings/intentions) are the literal joint-attention + social-cognition domains of M-CHAT-R/F and SRS-2. **What keeps CI-25 buildable and #4 vetoed is substance, not the "log" label:** (a) each prompt = a **single concrete parent-observable action in the parent's voice** ("I noticed [child] did not look over when I pointed at the dog"), **never a trait/rating/interpretation** ("lacks joint attention," "struggles with empathy"); (b) the prompt SET must **not reproduce an instrument's item structure/subscale grouping** — 3-5 plain behaviors per cluster ordered for recall, NOT mapped to SRS/M-CHAT subscales; (c) **every cluster tile + prompt is clinical-lead-reviewed string-by-string before ship**. Yes/Sometimes/Not-noticed must **never** be counted, color-coded, or surfaced as "more concerns here" — that is when a log becomes an unvalidated screen (VETO). **Banned labels:** "screener," "joint attention score/level," "perspective-taking deficit," "theory of mind," "social red flags," "autism," "ASD," "spectrum," "on the spectrum." **Allowed cluster label:** "Social communication" / "Connecting and playing with others" as a neutral grouping of observable behaviors only.

**3. NARROWING (ii) — Contextual surfacing honors the age-7 EF floor + reads as an invitation, never a verdict (peds, binding).** Trigger #2 (monitoring SectionCard "Start your observation log" when deriveMonitoring elevated:true) is OK as a **calm optional parent-initiated link** — no alert, no "concern detected," routes to logging not a verdict. Hold: (a) invitational copy ("A place to note what you're seeing over time," never "We noticed a problem"); (b) inherits the **age-7 floor** — below 7, attention/EF behaviors are logged as observation only, never an implied watch-area on attention alone (short attention / transition-rigidity at 5-6 are developmentally common); (c) the Ask-Arbor deep-link (trigger #3) coach string must pass the output-floor (no "this sounds like autism/ADHD," no false-reassurance). Keeping it OFF the primary bottom-nav (calm secondary surface, not clinical-primacy) is correct — hold it.

**4. Provider-brief export (Flow 4) — clears as parent-owned notebook; header is load-bearing (peds + safety).** Arbor structures the parent's OWN observations into a document the **parent owns and chooses to bring** to **their own** provider — **no Arbor-computed %, gap, count-as-finding, verdict, signature, or "assessed/screened" framing.** Binding: (a) export carries **only** the parent's entries + free-text, never an Arbor-generated summary/pattern-read/"areas of concern" section (an AI summary would re-trigger #4 + the VETO line); (b) header approved verbatim: *"Parent observation log — prepared by [name] for a conversation with [child name]'s provider. Not a diagnostic document."*; (c) **L3 confirmation** + **arbor-safety COPPA on the egress path** (child-data egress to a third party; parent governs it, per DEM-005 trap 3 / PHI-08). If it ever routes to an Israeli ועדה/סייעת committee it inherits the CEO-gated clinician-export decision (#7/#10) + a separate egress-consent gate — out of near-term scope; keep it pointed at "your provider," not an adjudicating committee.

**5. Assessment-prep guidance page (Flow 5) — clears as cited public guidance; no instrument names, no self-screen (peds + slp).** Calm surveillance-voice content ("what to bring, what to expect, you know your child best") grounded in CDC LTSAE / AAP surveillance-vs-screening. **Banned:** naming/reproducing any instrument (M-CHAT, SRS, ADOS, Vineland, ABAS, ASQ), any "do this checklist to see if your child needs an assessment" self-screen, any condition framing of what the assessment is "for." Routes the parent TO a real professional; never previews a verdict.

**6. Per-flow disposition (substance, not the "non-diagnostic" label):**

| Flow / element | soundness | claims | Disposition |
|---|---|---|---|
| Cluster prompts (Attention / Sensory / Language) | concerns (clearable) | none | clinical-lead reviews every string (parent-voice action, no rating-sum, no color-code); Attention inherits age-7 floor |
| **Social communication cluster (joint attention + perspective-taking)** | **concerns (clearable, narrowed)** | none | **the #4-VETO line**: raw observable actions ONLY, no SRS/M-CHAT item-structure clone, no trait/interpretation, no counted/ranked output; string-by-string review = build-ready precondition |
| Yes/Sometimes/Not-noticed responses | concerns (clearable) | none | **flat, un-aggregated, never summed/scored/color-coded** — rolling up to "more concerns here" = unvalidated screen → VETO |
| Dated timeline (Flow 3) | pass (narrowed) | none | parent's OWN entries + free-text only; **no intensity score, no aggregated count, no trend chart** (CI-24 §2) |
| Provider brief export (Flow 4) | concerns (clearable) | none | parent-owned, deselectable, header verbatim, **no Arbor-authored summary**; L3 + arbor-safety COPPA on egress |
| Assessment-prep guidance (Flow 5) | pass (narrowed) | none | cited public guidance only; **no instrument named, no self-screen** |
| Contextual surfacing (triggers #2/#3) | concerns (clearable) | none | calm optional link, no alert; age-7 floor; coach string passes output-floor |
| ObservationEntry child-data write | n/a | n/a | **NET-NEW write → independent arbor-safety COPPA review** |

**7. Output-floor (screenModelOutput) — REQUIRED conditionally.** If prompts/labels/timeline/export are **100% static/curated** (no model authorship), CI-25 does not itself trigger the floor — but still requires (a) clinical-lead string review + (b) a build-time lint for condition-name + effect-verb + instrument-name tokens across prompts, labels, the guidance page, AND the export template. **If ANY string is model-authored** (an Ask-Arbor coach line deep-linking here, an AI brief summary — itself banned per §4, any "what you're noticing" personalized text), it MUST pass `screenModelOutput` (`safety/outputScreen.ts`) with `CONDITIONS` extended to `autism`, `ASD`, `spectrum`, `joint attention deficit`, `theory of mind`, `social communication disorder`, `perspective-taking deficit` + a passing test asserting a synthetic string with an inferred label ("this looks like autism") is caught and replaced with the safe fallback. **Given the social-communication sensitivity, the board recommends binding screenModelOutput on the export-builder even if currently static** (defense-in-depth) — future model authorship must not ship a label without the screen.

**Lens splits:** **slp — concerns→pass on (i)** (social-communication cluster survives only as raw un-grouped observable actions, no SRS/M-CHAT structure clone; language prompts log the action, never interpret comprehension — lint `understands`/`follows directions` per CI-22 §2). **peds — concerns→pass on (ii)** (age-7 floor on contextual surfacing; no instrument on the prep page; the brief is a notebook, never an Arbor finding). **psych — pass, rubric-positive** (parent-register, parent-owned, routes OUT to a real provider = competence/responsibility/truth; no-score/no-verdict/no-color-code keeps it non-pathologizing + attachment-safe; calm-secondary IA correct — a child is observed by their parent, not screened by an app).

**screenModelOutput enforcement: REQUIRED if any string is model-authored; recommended bound on the export-builder as defense-in-depth.**

**cited_basis:** Assessment-Layer partial-VETO (this file — #4 SRS construct-clone VETO + M-CHAT hard rule + observation-vs-assessment line); CLINICAL-GUARDRAILS-CI-22-23-24 (firewall §0 ban on "screens/assesses/evaluates"; age-7 floor; condition-name + effect-verb bans; CI-24 §2 chart leak; parent-owned-export stance); CDC "Learn the Signs. Act Early." 2022 (Zubler et al., *Pediatrics* 2022 — surveillance, parent-to-provider, checklists 2mo-5yr only, no anchor for a 1-12 social-communication/perspective-taking output); AAP surveillance-vs-screening (Lipkin/Macias 2020); ASHA Scope of Practice (social-communication interpretation reserved to a licensed professional); SRS-2 = copyrighted autism instrument (joint attention + social cognition subscales); M-CHAT-R/F = validated toddler autism screen (link out only, never embed/paraphrase/score). **Honesty flag:** there is NO free public CDC/AAP/ASHA validated parent-administered joint-attention or perspective-taking instrument for ages 1-12 — which is precisely why CI-25 must stay a raw descriptive log and may never compute, group, count, or interpret these observations; the moment it does it becomes an unvalidated autism screen and flips to VETO.

**Verdict:** soundness:**concerns(clearable, social-communication cluster narrowed)** / claims:**none** / riskClass:**gated** → **BUILD-READY-NARROWED.** Buildable on the narrowed scope: (i) social-communication prompts are raw observable actions only, no SRS/M-CHAT structure clone, responses never summed/scored/color-coded, every string clinical-lead-reviewed; (ii) contextual surfacing honors the age-7 floor + invitational copy; export is parent-owned with NO Arbor-authored summary + verbatim header + L3 + arbor-safety COPPA on egress; prep page names no instrument + no self-screen; build-time lint for condition/effect/instrument tokens; screenModelOutput bound on any model-authored string. Final build-ready preconditions: the social-communication prompt-string review + (if model-authored) the output-screen test + arbor-safety COPPA on the ObservationEntry write/egress path. Routed to **arbor-pm** + through `arbor-orchestrator` (gated, identical to an arbor-safety veto on the held preconditions). "Non-diagnostic wording" + the self-declared header were ruled on substance — the design clears, the social-communication cluster narrows.

## Clinical GATE — 2026-06-23 — CI-28 Goal Builder + parent-concern→goal spine → soundness:concerns(clearable) / claims:none / riskClass:gated → BUILD-READY-NARROWED

**arbor-clinical-lead** + peds/slp/psych ran the binding gate on **CI-28** (typed goal selection UI: plain-language concern → 1-3 curated named goals → capability-cluster domain → `activeGoals` on ChildProfile → Daily Play selector at 1.6x weight; silently links BehaviorLogs to activeGoals by domain match and surfaces an "X observations linked" count). **Reuses, does not re-derive:** the binding bar in `mesh/improvement/CLINICAL-GUARDRAILS-CI-22-23-24.md` (firewall §0; condition-name + effect-verb bans; CI-24 §2 child-metric-chart leak; CI-22 "observations over time" cleared frame; output-floor precondition) and this file's observation-vs-assessment line (the Assessment-Layer gate below), Blueprint #91 Parent-Concern Intake + #95 count-not-metric, CI-25 referral rail (non-auto-firing, numbers source-verified live). **Verdict: soundness:concerns(clearable) / claims:none / riskClass:gated → BUILD-READY-NARROWED.** Not a clinical VETO.

**1. Why this CLEARS where the Assessment Layer was VETOED — it is on the buildable side of the line.** CI-28 **emits nothing about the child**: no score, no verdict, no percentage, no risk-map, no norm-cutoff, no instrument construct-clone (no SRS/Vineland/ABAS/Sensory-Profile), no condition inference (no autism/ADHD), no effect-size claim, no trend chart. The parent **explicitly selects** a goal from a finite curated list (no auto-assignment; concern pre-fill highlights but never pre-selects — honors "no goal assigned without explicit parent selection"). The "X observations linked" surface is a **flat count of the parent's OWN logged moments** matched by domain — a logging/recall artifact, the cleared #95 "you noted X this week" + CI-22 "observations over time" frame — NOT a child metric, NOT progress-toward-goal, NOT a verdict. The 1.6x Daily-Play weighting is a content-selection mechanism (which activities surface), not a measurement of the child. This is the descriptive, parent-owned, produces-nothing-about-the-child side the board has blessed four times. **No VETO.**

**2. The three required narrowings (build-ready-narrowed, binding before build-ready):**
- **(A) The goal-label list IS the gate — clinical-lead must review the actual 8-10 strings + the concern→goal mapping table before ship.** Each label must name a **behavior/situation/competency**, never a condition or an effect. Allowed (behavior-noun): "Following multi-step instructions," "Settling at drop-off / easing separations," "Taking turns and sharing," "Trying new foods," "Winding down at bedtime," "Naming and managing big feelings," "Building early talking / back-and-forth," "Switching between activities without a meltdown." **Banned in any label/tile/tooltip/empty-state:** any condition name (autism, ADHD, anxiety, "separation anxiety," SPD, ARFID, dyslexia, speech/language delay, dysregulation, processing disorder); any effect-verb on a child capacity (improves/builds/boosts/trains/strengthens/develops/reduces); "assess/screen/evaluate/measure/track [the child]." Use behavior-noun not condition-noun (reuse CI-23 §3: "settling at drop-off," never "separation anxiety").
- **(B) The "observations linked" count stays a FLAT COUNT — never a score, %, progress bar, completion ring, streak, or trend line.** A count rendered as "70% to goal" / "goal progress" / a sparkline = the CI-24 §2 child-metric-chart leak (an implicit per-child progress/competence trend = a verdict). Render only "X observations linked" / "No observations linked yet." No graduation %, no "goal achieved" verdict on the child. (The spec already states "no goal progress score or diagnostic language anywhere" — hold it verbatim; this fix makes it binding and extends it to the count surface.)
- **(C) The concern→goal reframe path inherits Blueprint #91 + the output-floor.** If ANY free-text concern (onboarding `CONCERNS` pre-fill, or a typed concern) is mapped to a goal by a model/AI string, that string MUST pass `screenModelOutput` (`safety/outputScreen.ts`) with the `CONDITIONS` regex + a passing test — never a verdict in either direction ("sounds like autism/ADHD" OR false-reassurance "he's fine"). A **speech/developmental** concern must additionally offer the **non-auto-firing CI-25 referral rail** ("worth raising with your pediatrician or an SLP," the four ASHA/CDC numbers source-verified LIVE per CLI-05 discipline) — the concern must NOT be silently swallowed into a play-goal when it warrants a provider conversation. Reuse the existing rail string; do not invent a new one; do not auto-fire it.

**3. Firewall + procedural (hold, all currently clean):** all Goal Builder copy is "developmentally informed, grounded in CDC/AAP/ASHA/WHO" — never "clinical/clinically validated/clinician-reviewed/assesses/screens/evaluates," no adviser/thinker identity (firewall §0 / CHARTER §3 p11). The toast/why-line copy ("Picked because you're working on [goal] with [name]") is claim-free mechanism language — clean, approvable. **`activeGoals` on ChildProfile is a NET-NEW child-data write → independent `arbor-safety` COPPA review on the write path** (gated regardless of the clinical clear, identical to the CI-23 observation-event + CI-24 emotion-check-in write paths).

**Lens splits:** **peds — pass-conditional** (no milestone/red-flag math here; goal labels stay behavior-nouns, no age-norm implied by a label). **slp — concerns→pass on (C)** (a "build early talking" goal must route a real speech concern to the CI-25 rail, not absorb it; numbers source-verified live). **psych — pass, rubric-positive** (explicit parent selection + parent-mediated + routes to a shared real-world activity = competence/order/responsibility-aligned; the flat-count-not-score narrowing keeps it attachment-safe and non-pathologizing — a child is not a project with a progress bar).

**screenModelOutput enforcement: REQUIRED** on the concern→goal reframe path (C) IF any string is model-authored. If the goal-label list and mapping are 100% static/curated strings (no model authorship anywhere in CI-28), the output-floor is not triggered by CI-28 itself — but the static label list still requires the clinical-lead string review (A) and the build-time lint (condition-name + effect-verb bans) before build-ready.

**cited_basis:** CDC "Learn the Signs. Act Early." 2022 (Zubler et al., *Pediatrics* 2022 — surveillance, parent-to-provider, no app verdict); AAP surveillance-vs-screening (Lipkin/Macias 2020); ASHA Scope of Practice + public developmental norms (the CI-25 referral windows); CHARTER §3 p11 + advisory.md §0 (firewall); reuses CI-22/23/24 binding guardrails + this file's observation-vs-assessment line + Blueprint #91/#95 verbatim. **Honesty flag:** the concern→goal mapping is a product-curation choice, not a validated taxonomy — it is defensible precisely because it produces no measurement; the moment a label or count implies a measured deficit/progress it crosses into the device-adjacent class and flips to VETO.

**Verdict:** soundness:**concerns(clearable)** / claims:**none** / riskClass:**gated** → **BUILD-READY-NARROWED.** Buildable with the narrowed scope: (A) clinical-lead reviews the actual label list + concern-mapping (behavior-noun, no condition, no effect-verb); (B) flat observation COUNT only, never a score/%/progress/trend; (C) concern reframe passes the output-floor + offers the non-auto-firing CI-25 rail for speech/dev concerns; plus arbor-safety COPPA on the `activeGoals` write path. The build pod may proceed on the narrowed scope; the label-string review + (if model-authored) output-screen test are the final build-ready preconditions. Routed to **arbor-pm** as a targeted finding + through `arbor-orchestrator`; "non-diagnostic wording" was ruled on substance and the design clears it.

## Clinical GATE — 2026-06-23 — CI-33 Sensory & comfort PREFERENCE log (`CI-33-sensory-comfort-log`) → soundness:concerns(clearable) / claims:none / riskClass:gated → BUILD-READY-NARROWED

**arbor-clinical-lead** + peds/slp/psych ran the binding gate on the **CI-33 build-ready design** (`SensoryComfortLog` card in My Child > Development; two clearable sublogs "What helps [child] feel calm" + "What tends to feel like too much for [child]"; `useChildCollection` keys `sensory-comfort`/`sensory-overwhelm`; silent `sensoryHint` feed to CI-30). **Ruled against the pre-set bar — NOT re-derived:** Blueprint #62 (this file L215 — "cleared a *what-helps-her-feel-comfortable* LOG, ruled `sensory profile` a label-leak, banned any count/threshold/verdict over logged dislikes"); the FDI "Sensory & Emotional Regulation **Map**" VETO row (L215, #6 — a "Map" = VETO, the *log* = clearable); CLINICAL-GUARDRAILS firewall §0 + observation-vs-assessment line (L196); backlog spec L2381-2404 (#62 sensory label-leaks). **Verdict: soundness:concerns(clearable) / claims:none / riskClass:gated → BUILD-READY-NARROWED.** The design IS the cleared shape — a descriptive, parent-owned comfort/trigger log that produces NOTHING about the child; two independent clearable lists; "no aggregate, count, or cross-sublog comparison ever computed or displayed" is the §62 line honored at design time. It is buildable to the narrowed scope below the moment the verbatim strings exist, the `sensoryHint` is confirmed count-free, and the output-floor is bound; it is **not** free-build until then.

**NO clinical VETO. No veto trigger present.** Confirmed clean against the standing trigger list: no per-child sensory score/verdict/index, no count/threshold over dislikes (the SPD-screen line), no condition name, no trend chart, no inference of the child's internal state, no kid-companion (parent-register throughout, no child-facing copy on this surface). The one veto-adjacent surface — the FDI "regulation Map" — is explicitly NOT what CI-33 builds; CI-33 is the log, which #62 cleared. The parent-mediated comfort log routes the parent toward their own hard-won soothing strategies (competence-positive); it never has Arbor classify, rate, or profile the child sensorially.

**Narrowed scope (build ONLY this — the gate clears this and no more):**
- **Two descriptive sublogs**, each an independent clearable free-text list (comfort = "What helps [child] feel calm" + optional "What helped" strategy field; overwhelm = "What tends to feel like too much" with NO "What helped" field — the design's own correct decision, holds). Conforms.
- **Optional category chip** (Sound/Touch/Light/Movement/Routine/Smell/Taste) labelled "Type (optional)" — NEVER "sensory category"/"sensory type." A descriptive tag on one entry, never aggregated. Conforms — hold the label.
- **No aggregate anywhere:** no count of entries rendered as a signal, no "X of N" line, no comfort-vs-overwhelm ratio, no per-category tally, no "your child tends to be sensitive to [X]" synthesis. This is the §62 SPD-screen line — binding build-time assertion: no aggregation/threshold/verdict code path over either sublog.
- **CI-30 `sensoryHint` (silent, back-end):** allowed ONLY as a back-end plan-texture routing value derived from the PARENT'S OWN logged comfort entries, never surfaced to the child or parent as a sensory read about the child, and **never derived from a COUNT/THRESHOLD** over the overwhelm log (e.g. "≥3 overwhelm entries → lower-stim" is the banned threshold-verdict — it must key off the *presence of a specific parent-logged preference*, a routing lookup, not a deficit tally). See required fix #3.

**required_fixes (binding before build-ready → soundness:pass):**
1. **Banned strings — enforced in EVERY string, label, tooltip, empty-state, category tag, AND the i18n HE/EN bundle (lint, not copy discipline):** `sensory profile`, `sensory seeker`, `sensory avoider`, `over-responsive`, `under-responsive`, `over-responsivity`, `under-responsivity`, `Sensory Processing Disorder`, `SPD`, `sensory processing`, `sensory sensitivity`/`sensory sensitivities`, `sensory diet`, `tactile-avoidant`/`tactile-defensive`, `vestibular`/`proprioceptive` (clinical-system nouns — the backlog problem-text "vestibular input" is back-end rationale ONLY, never user-facing copy). Build-time lint fails on any token in any locale.
2. **Empty-state + hint copy = descriptive-only, no interpretation (clinical-locked):** the proposed empty states clear as written — *"Add things you've noticed help [child] feel settled — a particular texture, a calming sound, a movement, a routine. It helps Arbor suggest activities that fit."* and *"Log things that tend to overwhelm [child] — knowing these helps you plan ahead and helps Arbor suggest lower-intensity activities when needed."* Hold them verbatim; no edit may add a "what this means about your child" read. The Today-surface hint *"what you log here shapes today's activities"* is fine (mechanism, no verdict). No copy may say "your child may have sensory sensitivities," "your child is sensitive to [X]," or any trait/threshold read.
3. **`sensoryHint` is count-free + never rendered:** binding build-time assertion — the CI-30 derivation reads the PRESENCE of specific parent-logged comfort preferences (a routing lookup), emits only `'lower-stim' | 'movement-first' | 'none'` to the plan generator, is NEVER surfaced on any child- or parent-facing surface, and contains NO count/threshold/ratio over the overwhelm log. A threshold like "N dislikes → flag" is the §62 SPD-screen line and is forbidden. (The spec truncated mid-sentence on this value — the build pod MUST confirm the implemented derivation is presence-keyed, not count-keyed, before build-ready.)
4. **Output-floor (precondition):** the displayed entries are PARENT free-text (the parent's own words, not Arbor output — no screen needed on the raw display). BUT if any model-authored string ever summarizes, categorizes, or "notices a pattern" across the log (none is in scope today; document that), it MUST pass `screenModelOutput` (`safety/outputScreen.ts`) with the `CONDITIONS` regex extended to catch the banned sensory tokens above + a passing `safety/outputScreen.test.ts` case. If the build ships zero model-authored strings, the floor is satisfied by construction — document it. Also sanitize free-text through the CONDITIONS lexicon before it echoes into any CI-30 derivation or card copy (copy-injection floor, CI-29 #3 precedent).
5. **arbor-safety COPPA co-sign (hard, parallel gate):** two new child-data write paths — `sensory-comfort` and `sensory-overwhelm` collections under the active child. Co-sign on the write path required before build-ready (backlog Gate L2398/L2404). Independent of the clinical clear.
6. **slp AAC/non-exclusion floor:** free-text is the input; the optional category chip provides a low-literacy/icon path. Confirm the parent can log with the chip + short text only (no long-form required). Hold the design's parent-register-only stance (no child-facing copy on this surface).

**Lens splits:** **peds — pass, conditional on the no-aggregate assertion (#3):** a descriptive comfort/trigger log is the correct surveillance-adjacent posture (SPD is not a DSM-5 diagnosis; AAP cautions against it as a stand-alone dx, 2012; there is NO CDC anchor for sensory-sensitivity interpretation — which is *why* a count/threshold cannot be validly computed and must stay descriptive). **slp — pass, conditional on #1/#6:** the log trends parent-observed comfort, never an oral-motor/sensory-integration construct; the backlog correctly routes oral-motor/motor-planning nodes to a SEPARATE Domain-A review (out of scope here — hold that boundary). **psych — pass, conditional on #2:** parent-register, descriptive, competence-positive (hands the parent back their own soothing strategies); the only residual risk is an interpretation leak in copy/hint, locked out by #2/#3.

**cited_basis:** Blueprint #62 cleared "what-helps-feel-comfortable" LOG + banned `sensory profile`/count/threshold/verdict (this file L215; backlog L2392); FDI "regulation Map" VETO (L215 #6 — Map=VETO, log=clearable); CLINICAL-GUARDRAILS firewall §0 + observation-vs-assessment line; AAP SPD-caution (2012 — SPD not a stand-alone DSM-5 dx); CDC LTSAE 2022 (no anchor for sensory-sensitivity interpretation — Zubler et al., *Pediatrics* 2022); EU MDR Rule 11 / FDA SaMD (a per-child sensory verdict/index = device-adjacent, the drop rationale); advisory.md §0 / CHARTER §3 p11 firewall ("developmentally informed," never "clinical/assesses/screens").

**Verdict:** soundness:concerns(clearable) / claims:none / riskClass:gated → **BUILD-READY-NARROWED.** Buildable to the narrowed scope above the moment (i) the 6 required_fixes are in the actual strings/logic, (ii) the no-aggregate + count-free-`sensoryHint` + output-floor assertions are green and tested, and (iii) `arbor-safety` co-signs the two child-data write paths. Until then it stays GATED — not free-build. Routed through `arbor-orchestrator` as build-ready-narrowed-pending-cosign, identical to an arbor-safety conditional clear. The motor capability nodes in the broader CI-33 backlog ticket (Domain E, `observableSignals`) are a SEPARATE clinical-lead string pass and are NOT covered by this clear.

## Clinical GATE — 2026-06-23 — CI-30 Daily Plan Generator ("best 15-min activity today") → soundness:concerns(clearable) / claims:none / riskClass:gated → BUILD-READY-NARROWED

**arbor-clinical-lead** + peds/slp/psych ran the binding gate on **CI-30** (goal-linked + interest-themed + energy-aware daily activity recommendation in the existing `daily-play` tab via a new `DailyPlanCard`, + an inline 1-tap post-activity observation that writes to the child's goal sub-collection; depends on CI-27/28/29 landing in the same run). **Reuses, does not re-derive:** the binding bar (`mesh/improvement/CLINICAL-GUARDRAILS-CI-22-23-24.md` §0 — firewall, condition-name + effect-verb bans, CI-24 §2 child-metric-chart leak, CI-22 §1/§2 observation-logs-action frame, output-floor precondition), the **CI-28 gate above** (behavior-noun-not-condition-noun goal labels + flat-count-not-score), the 100-Blueprint PASS-in-principle (#91 reframe-to-goal / #95 descriptive non-metricized recall), CLI-06 (`screenModelOutput` output-floor). **Verdict: soundness:concerns(clearable) / claims:none / riskClass:gated → BUILD-READY-NARROWED. No clinical VETO.** The wireframe scope already places CI-30 in Bucket-1 (safe spine, no developmental claim); this gate confirms that read and pins the narrowing.

**1. Why no VETO — CI-30 is on the buildable side of the observation-vs-assessment line.** It is a **recommendation engine + a descriptive parent observation log**. It computes **nothing about the child**: no per-child score, verdict, risk-map, percentage, "on-track," instrument construct-clone (no SRS/Vineland/ABAS/Sensory-Profile), no condition inference (no autism/ADHD), no effect-size claim, no intensity/frequency trend chart. The activity ranking reuses the existing `rankDailyPlay` logic keyed to the goal's `capabilityDomain` — recommending an activity is content selection, not measuring a child. This is the "best 15-min activity today, NOT diagnose my child" north star the board has blessed four times. None of the veto triggers fire.

**2. The two clinical surfaces (the gate) — both CLEARABLE with the narrowings:**
- **(A) The why-line is a PROVENANCE string, not an effect claim — clearable as mechanism, must pass the output-floor + inherit the CI-28 goal-label firewall.** "Matched to your goal — calm morning departures — and [child]'s love of trains" states *why this was chosen*, never "this will improve/calm/build" the child — the allowed register (CI-28 §3 "Picked because you're working on [goal]"). BUT it is **template-/model-composed from the CI-28 goal label + the CI-29 interest**, so: bind it to `screenModelOutput` (`safety/outputScreen.ts`) with the `CONDITIONS` regex + a passing test (CLI-06/CI-22/CI-24/Blueprint precedent — a declared-clean template is not a guard); and **the goal label is the leak vector** — if a condition-named goal reaches the why-line it renders the condition verbatim, so the CI-28 behavior-noun-not-condition-noun gate is load-bearing here and the why-line must screen the interpolated label before render. **Banned in the why-line / any plan copy:** effect-verbs on a child capacity (improves/builds/boosts/trains/strengthens/develops/reduces) and any "this activity will [calm/fix/reduce] [child]." The SPARSE-DATA "Sharpens as you log more days" line is clean (the *recommendation* sharpens, not the child) — do not let it drift to "sharpens [child]'s skills."
- **(B) The post-activity observation stays a parent-attributed descriptive note — must NOT become a scored/trended goal metric; + arbor-safety COPPA on the write path.** The 1-tap log ("What did you notice during the activity?", free-text ≤200 chars → goalId/capabilityNodeId/observationText/timestamp) is the right, sound primitive (child-as-own-baseline, parent-attributed, descriptive — CI-22 §1 / Blueprint #95). **The binding narrowing:** the observation feeds the goal as a *descriptive note only* — it may **never** be aggregated into a per-child **goal-progress score, %, completion ring, "goal achieved"/"on track" verdict, or an intensity/frequency trend chart** (the CI-24 §2 + CI-28 leak: a trended child metric = implicit assessment). The "observation logged" badge + the goal recall are fine; "[child] is X% to this goal" / "improving on this goal" is the veto line. Free-text → child-data event → **independent `arbor-safety` COPPA review on the write path** (reuse the CI-23 observation-event / CI-24 emotion-check-in / CI-28 `activeGoals` write precedent). Toast "Nice — added to [child]'s record" is clean (records, does not score).

**3. Secondary holds (clearable in-copy/config) + firewall:**
- The `GoalsCard` "Get today's plan" affordance and the done-state must render **no child-progress score/%/"on track"** — descriptive recall only.
- Duration/energy chips (CI-31: 8/15/25 min) + the weekend "extended" pre-select are **parent-preference settings, no developmental claim — no clinical hold.**
- The **Coach shortcut** pre-fills the existing coach input → inherits the CLI-06 `screenModelOutput` guard already required on the coach path; no new surface.
- **Firewall §0:** all plan/why-line/empty-state copy "developmentally informed, grounded in CDC/AAP/ASHA/WHO," never "clinically validated/clinician-reviewed/assesses/screens/evaluates," no adviser/thinker identity; the why-line is *provenance*, never an endorsement (CHARTER §3 p11).

**Lens splits:** **peds — pass-conditional** (recommendation + descriptive log carries no milestone/red-flag verdict; only watch = the observation never hardening into a scored goal trend). **slp — pass-conditional** (if a goal touches communication, the observation prompt logs the *observed action*, never "understands/comprehends/articulates better" — the CI-22 §2 comprehension-leak + no-effect-on-language lint applies to the observation free-text; a persistent communication concern routes to the reused, non-auto-firing CI-25 rail, never invented here). **psych — pass, rubric-positive** (the why-line routes the parent AT a shared real-world activity = parent-mediated, attachment-safe, competence/order-building, never a kid-companion; the observation stays the parent's own note, never a child-as-project progress score).

**screenModelOutput enforcement: REQUIRED** on the why-line (it interpolates the CI-28 goal label + CI-29 interest, so it is treated as model-/template-composed) and inherited on the Coach path. Screen the rendered string + add the test.

**cited_basis:** CDC "Learn the Signs. Act Early." 2022 (Zubler et al., *Pediatrics* 2022 — surveillance, no app verdict); AAP surveillance-vs-screening (Lipkin/Macias 2020 — observe/route, never score); ASHA Scope of Practice (communication-progress reserved to a professional; software recommends activities, does not assess); CHARTER §3 p11 + advisory.md §0 (firewall — provenance not endorsement). Reuses CI-22 §1/§2, CI-23 §3, CI-24 §2, CI-28, Blueprint #91/#95, CLI-06 verbatim. **Honesty flag:** activity-matching to a goal is a product-curation choice, not a validated intervention — defensible precisely because it produces no measurement and asserts no effect; the moment the why-line or the observation implies a measured gain/progress it crosses into the device-adjacent/effect-claim class and flips to VETO.

**Verdict:** soundness:**concerns(clearable)** / claims:**none** / riskClass:**gated → BUILD-READY-NARROWED.** Buildable in this run **only** with: (1) why-line bound to `screenModelOutput` + CONDITIONS + test, and the CI-28 goal label held to behavior-noun-not-condition-noun (no condition name reaches the why-line); (2) the observation stays a descriptive parent-attributed note — **no goal-progress score/%/trend-chart/"on track" verdict on the child anywhere**; (3) arbor-safety COPPA clears the observation write path; (4) effect-verb + comprehension-leak lint on the why-line and observation; (5) firewall §0 on all copy. Cleared to `soundness:pass` on a final board copy pass once those are in the actual strings. Routed to **arbor-pm** as a targeted finding + through `arbor-orchestrator` (the narrowing is binding on the build pod, identical handling to an arbor-safety condition); "non-diagnostic wording" was ruled on substance and the design clears it.

## Clinical GATE — 2026-06-23 — "Developmental Functioning Assessment Layer" (ABAS/Vineland/SRS/Sensory-Profile-ideational + ועדה/סייעת committee report) → soundness:VETO(partial, the deficit-quantification spine) / claims:UNSUBSTANTIATED / riskClass:device-adjacent → BLOCKED-FOR-BUILD

**arbor-clinical-lead** + peds/slp/psych ran the binding gate on a third strategic input: a 10-capability "Developmental Functioning Assessment Layer" EXPLICITLY "ideationally based on" the same copyrighted instruments the board partially-VETOED in Framework-v2 (ABAS/Vineland/SRS/Sensory Profile), claiming to mimic their VALUE without verbatim copying or diagnosing. Flagship = a quantified-deficit report ("% of time needing adult mediation," "areas of difficulty in social communication," "significant gap") for the Israeli **ועדה** (eligibility/placement committee) + **סייעת** (classroom aide) request — i.e. an app-generated number allocating a public special-education resource. **Reuses, does not re-derive:** Framework-v2 partial-VETO (MEMORY.md L176-178 — Assessment-Engine spine vetoed on IP + EU-MDR Rule 11 + scope-of-practice; M-CHAT hard rule); 100-Blueprint PASS-in-principle (this gate, L101-142 — diagnosis-free is what made *that* input clearable); CLINICAL-GUARDRAILS-CI-22-23-24.md (firewall §0, age-7 EF floor, condition-name + effect-verb bans, parent-owned-export stance). **All three lenses converged independently: recommend-VETO on the deficit-quantification spine.**

**1. The core ruling — observation-vs-assessment, the exact line.** The board has drawn this line three times and it catches this input cleanly. The **buildable** side (blessed) = a **descriptive, parent/teacher-owned observation LOG that produces NOTHING about the child** — child-as-own-baseline, behavior-anchored, parent-attributed ("you noticed [child] needed help to switch tasks 3 times this week"), strengths-AND-needs, no aggregation, no rating-scale, no norm-cutoff, no verdict (CI-22 §1 / Blueprint #62/#67/#91 frame). The **vetoed** side = a **deficit-quantification ENGINE that emits a per-child score/verdict/index** — three-tier "functioning map," "%-time," "areas of difficulty," "significant gap." **"Ideationally based on Vineland/ABAS/SRS/Sensory Profile" does NOT survive:** cloning an instrument's *construct + item structure + scoring* is validity-theft + device-adjacency *independent of verbatim copying* (CI-23 §2 holding reused — invoking the instrument "implies the evidence without the delivery conditions that produced it"; reproducing a licensed examiner-administered instrument's classification output *without* its standardized administration borrows its authority while discarding its validity, which is worse than copying). A per-child functional-deficit inference is EU-MDR Rule 11 / FDA SaMD (Cognoa precedent); **a "functional, not diagnostic" disclaimer does not declassify it** (Framework-v2, verbatim). The veto attaches to **what the surface DOES — emit a per-child quantified deficit verdict — not to whether the word "diagnosis" appears.**

**2. #4 Social Communication Screener — recommend-VETO, no buildable screener version exists (SLP lead).** Its 6 items map almost one-to-one onto the SRS-2's published subscales (initiates play / responds to peers / shares experience = Social Communication + Social Motivation; plays mostly alone = Social Motivation; stuck on one topic = Restricted Interests/Repetitive Behaviors; understands turns = Social Cognition) — that is the SRS construct reproduced, not "inspired by." Vetoed on **three independent grounds, any one sufficient:** (a) **SRS construct-cloning** (IP + validity-theft even with zero verbatim items); (b) it **functions as an unvalidated autism screen** — an unnormed 6-item reskin of an autism instrument is *worse* than M-CHAT (which at least has known ~50% PPV), and the **M-CHAT hard rule** (never embed/paraphrase/score an autism instrument in-app — link out only) applies by analogy; (c) it **pathologizes normal social-communication variation** — solitary/parallel play (typical through ~3-4), circumscribed interests, and bilingual/temperamentally-reserved presentations will mass-false-flag. The word **"screener" is itself a firewall-banned verb** (CI §0 bans "screens/assesses/evaluates"), and **"areas of difficulty in social communication" as a structured OUTPUT is the veto line regardless of the "not a diagnosis" hedge.** Allowed near it (and ONLY this): a parent/teacher **behavior log** that outputs nothing about the child + the **non-auto-firing CI-25 referral rail** ("worth raising with your pediatrician or an SLP," numbers source-verified live) + AAC/minimally-verbal-inclusive prompts + a **parent-owned export** the family carries to *their own* provider/ועדה.

**3. #3 Adult Mediation Index → committee report — recommend-VETO; the worst item in the set (peds + psych).** An app-generated quantified **"% time needing adult mediation"** entered into an official ועדה decision is **(c) a regulated/liability surface AND (b) an unlicensed functional assessment.** Three failure modes: **false precision** (a percentage with no validated denominator, no inter-rater reliability, no norm sample, read by a committee as data when it is a logging artifact); **device-class hardened by official use** (a per-child quantified functional-deficit index used for an eligibility determination is the Rule 11 / SaMD "information used for decisions with diagnostic/therapeutic purpose" class — official-use *raises* the classification); **liability** (Arbor authored the artifact that drove a real-world public-resource allocation; over-states → wrong aide + a label that follows the child, under-states → a needing child denied). **The line:** a *teacher's own* contemporaneous, signed narrative log is a normal part of a ועדה packet — Arbor structuring the teacher's/parent's OWN observations is fine; an **Arbor-computed index/score/percentage** is Arbor asserting a measurement it has no instrument to make — that is the crossing.

**4. #7 Professional Report Generator + #10 Parent-Teacher Shared View — re-touch the HELD, CEO-gated clinician-export decision (DEM-005 / #97); surface to Guy, not a near-term build.** The ועדה report + the **gan-teacher as a data-entry user feeding a committee-facing report** is the clinician-facing export / Professional-Mode console that is **CEO-gated at 12 months** (this gate L121/L138; DEM-005 L146). It is *harder* than #97 on two axes: the recipient is an **official public committee allocating a resource (סייעת)**, not the family's own provider — so the report functions as an **assessment instrument in an adjudication** (unlicensed-assessment scope + firewall breach, Arbor reading as assessor-of-record); and a teacher entering child data that **egresses to a government committee** is the deepest third-party child-data path in the product — a separate hard `arbor-safety`/Guy **COPPA + child-data-egress consent gate** the *parent* must own and govern (DEM-005 trap 3 / PHI-08), not the teacher and not Arbor. **The ONLY sound near-term form** = a **parent-owned, zero-metric, zero-verdict observation export** (Arbor structures the parent's/teacher's OWN descriptive observations into a document the parent owns and chooses to bring; **no Arbor-computed %, no gap, no verdict, no signature, no "assessed/screened" framing — Arbor is the notebook, not the assessor**) — and even that is **HELD** behind the CEO clinician-export decision + the egress consent gate.

**5. #9 Before/After "did the intervention improve functioning" — claims:VETO as worded; clearable ONLY as a parent-owned own-baseline trend.** "Did *the intervention* improve functioning" is the **claim-ceiling sentence verbatim** (no improves/builds/reduces on a child capacity; Blueprint claims-register L130) — Arbor asserting its **own intervention works** = unsubstantiated efficacy + implicit clinical-outcome measurement; and a before/after *functioning-score delta* is also a banned **trend-chart of a child metric** (CI-24 §2) + child-as-project framing (attachment-unsafe). **Allowed:** the family seeing **their own logged trend, parent-attributed, mechanism-only, no efficacy attribution, no scored delta** ("you noted [child] used more two-word phrases this week" — Blueprint #95). As written → VETO; re-scopable to a parent-owned descriptive timeline only.

**6. Claims register + per-capability verdict ("non-diagnostic/functional" wording does NOT auto-clear — ruled on substance):**

| # | Capability | soundness | claims | riskClass | Disposition |
|---|---|---|---|---|---|
| 1 | Child Functional Profile → map (independent/needs-mediation/**significant gap**) + NEED-FOR-MEDIATION | **VETO** | UNSUBSTANTIATED | device-adjacent (MDR R11) + IP | adaptive-behavior verdict + "gap" leak + Vineland/ABAS construct-clone |
| 2 | Transition Difficulty Tracker | **concerns (clearable)** | none | gated | clearable ONLY re-scoped to a descriptive per-event log (CI-22 "switch got stuck"), no "difficulty" rating, no trend chart |
| 3 | **Adult Mediation Index** ("% time needing mediation" → committee) | **VETO** | UNSUBSTANTIATED | device-adjacent + regulated/liability | app-asserted deficit metric allocating a public resource — the worst item |
| 4 | **Social Communication Screener** ("ideationally based on SRS") | **VETO** | UNSUBSTANTIATED | device-adjacent (M-CHAT-rule analogue) | SRS construct-clone + unvalidated autism screen + pathologizes normal variation; "screener" + "areas of difficulty" both banned |
| 5 | Attention & Participation → "general attention difficulty" | **VETO** | — | gated (condition-leak) | ADHD-adjacency; CI-22 bans even as disclaimer anchor; no EF-flag below age 7 |
| 6 | Sensory & Emotional Regulation **Map** ("ideationally based on Sensory Profile") | **VETO as a "Map"; clearable ONLY if stripped to Blueprint #62** | none if stripped | gated | Blueprint #62 cleared a "what-helps-her-feel-comfortable" *log* but ruled `sensory profile` a label-leak + banned any count/threshold/verdict; a regulation "Map" = VETO |
| 7 | **Professional Report Generator** (ועדה PDF) | **VETO as Arbor-authored deficit report; HELD as parent-owned log export** | UNSUBSTANTIATED | **CEO-gated + child-data-egress gate** | re-touches DEM-005/#97; inherits the veto of #1/#3/#4/#5 it aggregates; surface to Guy |
| 8 | Intervention Planner (visual schedule, timer, choices) | **pass (SAFE)** | none | safe | plain enrichment/structure tooling, no verdict, no claim — provided no copy says it "improves functioning" |
| 9 | Before/After "did the intervention improve functioning" | **concerns (clearable as own-baseline timeline only)** | **VETO if any effect/before-after-delta asserted** | gated | claim ceiling + scored-delta trend chart; re-scopable to parent-owned descriptive trend |
| 10 | Parent-Teacher Shared View (teacher → committee) | **concerns → HELD** | none if raw-log-only | **CEO-gated + COPPA egress gate** | a shared *descriptive* co-owned log is fine; VETO the moment it surfaces #1/#3/#4/#5 indices |

**Net it out:** **recommend-VETO ×5** (#1, #3, #4, #5, and #9-as-effect-claim) on the **deficit-quantification spine** — the Framework-v2 Assessment-Engine spine returning under "functional/non-diagnostic" clothes, now **aggravated** by official-resource-allocation use (the output is *adjudicative*, not enrichment — the strongest medical-device/scope-of-practice context the board has seen; an elevated risk class, not merely "the v2 spine again"). **gated-clearable-if-re-scoped ×3** (#2, #6, #10 — to descriptive logs that produce nothing about the child + output-floor bound + final board copy pass). **SAFE ×1** (#8 Intervention Planner). **Re-touches the CEO-gated clinician-export decision ×1** (#7 — HELD, surface to Guy). **The only salvageable product** = the **parent-owned descriptive observation log** (#8 tool + #2/#6/#10 re-scoped) the *family* carries to *their own* professional / ועדה — **Arbor is the notebook, never the assessor; Arbor computes no number, renders no verdict, and signs nothing.**

**cited_basis:** Framework-v2 PARTIAL-VETO (MEMORY.md L176-178 — IP / EU-MDR Rule 11 / FDA SaMD, Cognoa precedent / scope-of-practice / M-CHAT hard rule; "disclaimer does not declassify"); 100-Blueprint PASS-in-principle + claims-register VETO rows + #62/#91/#95/#97 + DEM-005/PHI-08 (this gate L101-146); CI-22/23/24 binding guardrails (firewall §0 "screens/assesses/evaluates" ban; age-7 EF routing floor; condition-name + effect-verb bans; parent-owned-export stance); SRS-2 = copyrighted 65-item autism screening/diagnostic-aid instrument, 5 subscales (WPS/PAR); CDC LTSAE 2022 surveillance-not-diagnosis (Zubler et al., *Pediatrics* 2022 — no anchor for adaptive-%/sensory/social-communication/6-10 attention); AAP surveillance-vs-screening (Lipkin/Macias 2020) + SPD-caution (2012); ASHA Scope of Practice (social-communication interpretation reserved to a licensed professional). **Honesty flag:** there is no CDC/AAP milestone anchor for any of the quantified outputs this layer proposes — which is itself why they cannot be validly computed and must remain descriptive logs.

**Verdict:** soundness:**VETO(partial — the deficit-quantification spine)** / claims:**UNSUBSTANTIATED** / riskClass:**device-adjacent → BLOCKED-FOR-BUILD.** Routed to **arbor-pm** as targeted findings (no backlog file written by the board) and through `arbor-orchestrator` as blocked-for-build, identical to an `arbor-safety` veto. #7 surfaced to Guy as the CEO-gated clinician-export decision. The "non-diagnostic / functional" wording is ruled on substance and **does not clear it.**

## Clinical GATE — 2026-06-23 — CI-27 Developmental Capability Graph + admin/seed + prohibitedDiagnosticClaims enforced via screenModelOutput (AP-071) → soundness:pass / NO VETO / claims:none / riskClass:safe → **BUILD-READY-NARROWED**

**Reused from prior verdicts — not re-derived.** CI-27 operationalizes the exact primitive the board already blessed in the "100 Capability Blueprint" GATE (L101/L136/L142): the 8-domain typed capability graph + the typed `prohibitedDiagnosticClaims` field = **soundness:pass-in-principle, claims:none, riskClass:safe** — SAFE *conditional on that field being enforced via `screenModelOutput`*. **CI-27 IS that enforcement build** + schema + seed, and the binding output-floor from CLINICAL-GUARDRAILS-CI-22-23-24.md §0/§17 made load-bearing at the schema layer (CLI-06 pattern). The standing device-adjacent veto lines (per-child score/verdict/gap-map, SRS/Vineland/ABAS clone, condition inference, effect-size claim) sit DOWNSTREAM — CI-27 computes no number, renders no verdict, and is the mechanism that *prevents* those leaks. The spine clears; four narrowings fence where the design brushes a veto line.

**Coordination:** peds (school-readiness-as-verdict guard, age-7 EF floor), slp (comprehension/language-leak terms in seed lists), psych (anxiety/emotion-label terms, attachment-safe card copy). No lens dissents; no VETO.

**Four binding narrowings (in the actual code, not asserted):**
1. **Merge, never replace, base `CONDITIONS`.** `buildConditionsForCapability()` OR's the per-capability addendum onto base `CONDITIONS`, never substitutes; the CI-22/23/24 floor always applies on every capability-context call; build-time assertion proves merged regex ⊇ base.
2. **`prohibitedDiagnosticClaims` non-empty AND floor-seeded for every AI-generation-eligible node; `capabilityAdmin.ts` hard-fails CI otherwise.** Seed = condition names per domain (A→language delay/apraxia; B→autism/ASD; C/D→ADHD/executive dysfunction/anxiety) + universal effect verbs + instrument names (BRIEF, Vineland, ABAS, SRS, Sensory Profile, M-CHAT, ASQ) + branded programs.
3. **Domain G `school_readiness` = enrichment content ONLY**, never a readiness score/verdict/index/percentile (= the device-adjacent construct-clone class the board VETOED: FDI-03/FDI-04/CI-02 family). observableSignals/progressionLevels = child-as-own-baseline behaviors, never normed rungs.
4. **DailyPlayCard "because…" line passes the output-floor and surfaces an observable signal, never a domain-label verdict.** Allowed (CDC surveillance voice); routes through `screenModelOutput` with the merged regex; no domain label, no "behind/at-risk/delayed," no effect verb.

**allowedCopy:** "Because you logged transition struggles — this is a chance to practice switching between rules and tasks together."; "what you've noticed over time"; "where to lean in this week, based on what you've noticed"; parentFriendlyDescription/observableSignals = a concrete observed behavior; "developmentally informed, grounded in CDC/AAP/ASHA/WHO" (never "CDC milestones" on a 6-10 surface); CI-25 referral rail "worth raising with your pediatrician or an SLP".

**bannedStrings (ruled on substance — "non-diagnostic wording" does NOT auto-clear):** per-child score/rank/percentile/verdict (readiness score, ready/not ready, school-readiness index, capability/EF score, dependence %, 0-N value, above/below age level); instrument clones as schema/output (SRS, Vineland, ABAS, Sensory Profile, BRIEF/BRIEF-P, M-CHAT, ASQ); condition inference (ADHD, attention deficit, executive dysfunction, autism/ASD, processing disorder, language delay, anxiety/anxiety disorder, depression, dyslexia, dysregulation-as-label, SPD); effect-size verbs on a child capacity (improves/builds/boosts/trains/strengthens/develops/reduces); assessor verbs (assesses/screens/evaluates/measures/tests/clinically validated/clinician-reviewed); branded programs (Triple P, Incredible Years, PCIT, Hanen, ESDM, DIR/Floortime, Zones of Regulation, Orton-Gillingham); comprehension-leak (understands/follows directions/comprehends as a logged judgment).

**thresholds (cited):** no new per-child cutoff (schema, not a screener). Inherited: no provider-routing/watch-flag on EF/attention below **age 7** (CI-22; Zubler et al. *Pediatrics* 2022 — CDC LTSAE stops at age 5; AAP Lipkin/Macias 2020). Communication-referral language reuses ASHA/CDC public norms (few/no words ~18m; <~50 words or no 2-word combos by 24m; ~75% intelligible by 3), source-verified live. School-readiness has no CDC/AAP age-normed readiness cutoff — why a readiness score cannot be validly computed and stays descriptive enrichment.

**requiredFixes:** (1) buildConditionsForCapability merges, never replaces, base CONDITIONS + superset assertion. (2) capabilityAdmin hard-fails CI on empty/floor-missing prohibitedDiagnosticClaims; seed from consolidated banned set. (3) Domain G seed enrichment-only, no readiness score/verdict/index/percentile, child-as-own-baseline signals. (4) DailyPlayCard "because…" line through screenModelOutput, observable signal not domain-label verdict, no effect verb. (5) outputScreen.test.ts asserts BOTH a seeded-node prohibited term AND a base-CONDITIONS term (e.g. "this looks like ADHD") are caught on the capability path (proves the merge). (6) arbor-safety reviews capabilityIds write path if a node observation feeds the child record (COPPA).

**screenModelOutput enforcement: REQUIRED** — the binding clinical condition; without fixes #1/#4/#5 green, NOT build-ready.

**cited_basis:** 100-Blueprint PASS-in-principle + "enrichment core SAFE once enforced via screenModelOutput" (L101/L136/L142); CLINICAL-GUARDRAILS-CI-22-23-24 §0/§17 + CI-22 age-7 floor + CI-23 branded/effect ban + CI-24 anxiety/intensity ban; CLI-06 pattern; device-adjacent VETO (MDR Rule 11/FDA SaMD, Cognoa); CDC LTSAE 2022 (Zubler et al. *Pediatrics* 2022, stops at 5); AAP surveillance-vs-screening (Lipkin/Macias 2020); ASHA Scope of Practice + public norms; advisory.md §0 firewall.

**Verdict:** soundness:**pass / NO VETO** · claims:**none** · riskClass:**safe** → **BUILD-READY-NARROWED.** Routed to **arbor-pm** as a targeted finding and through `arbor-orchestrator` as build-ready-narrowed. Build-ready confirms when requiredFixes are in the actual code + outputScreen.test.ts green + final board copy pass on seeded parentFriendlyDescription/observableSignals strings.

## Clinical GATE — 2026-06-23 — LANG-15 Parent Language Coaching track (Word World — serve-and-return / narrated play / shared book-reading) → soundness:concerns(clearable) / claims:none / riskClass:gated → BUILD-READY-NARROWED

**arbor-clinical-lead** + **slp (LEAD — parent-mediated language intervention is ASHA-scope)** / peds (age bands) / psych (attachment-safe framing) ran the binding gate on **LANG-15** (a parent-only `WordWorldTab` inside Practice Studio/HeroArcade: 3 strategy module cards → an age-matched "Today's Moment" prompt + context chip + "We tried this" log → a "This Week" descriptive moment list → an always-available, never-auto-fired CI-25 referral rail). **Reuses, does not re-derive:** the binding bar (`mesh/improvement/CLINICAL-GUARDRAILS-CI-22-23-24.md` §0 firewall + claim-ceiling + output-floor; **CI-23** §1/§2/§3/§4 verbatim — this IS a CI-23-class Arbor-native parent-coaching track on the communication domain; **CI-30** L194-214 recommendation-engine + descriptive-parent-log pattern; **CI-25** referral-rail string + the source-verify-numbers rule; **CI-27/AP-071** output-floor at the string layer). **Verdict: soundness:concerns(clearable) / claims:none / riskClass:gated → BUILD-READY-NARROWED. No clinical VETO.**

**1. Why no VETO — LANG-15 is on the buildable side of the observation-vs-assessment line, and the mechanism is the highest-evidence early-language intervention.** The capability **computes nothing about the child**: no per-child language score/verdict/percentage, no "on-track/delayed," no intelligibility %, no word-count, no SRS/Vineland/ABAS construct-clone, no autism/ADHD inference, no effect-size claim, no intensity/frequency trend chart. It is a **strategy-prompt recommender + a descriptive parent-action log** (CI-30 pattern). The "This Week" panel logs the **parent's own action** (a `lang-strategy` PracticeEvent: timestamp + module label), explicitly **NO child-language-output metric** — the design states this and it is the load-bearing narrowing. The serve-and-return / dialogic-reading mechanism is genuinely the most-replicated parent-mediated early-language mechanism (CDC/ASHA), and the design correctly **states the mechanism, never an effect size** — "these activities give you structured moments to do that" (CI-23 §1 ceiling), never "improves/builds [child]'s language." None of the §1 veto triggers fire. Rubric-positive: parent-mediated, routes the parent AT a shared real-world activity, never a kid-companion, attachment-safe.

**2. The clinical surfaces (the gate) — all CLEARABLE with the narrowings below:**
- **(A) Strategy-prompt text is PROVENANCE/mechanism, not an effect claim — clearable, must pass the output-floor IF model-/template-authored.** Prompts like "Wait three seconds after your child makes a sound — give them space to take another turn" are concrete parent-action instructions (allowed register). **Binding:** if any prompt, age-band match, or "Today's Moment" string is model-/template-composed (not a 100% static curated bank), bind it to `screenModelOutput` (`safety/outputScreen.ts`) with the `CONDITIONS` regex extended for language leaks (`language delay`, `apraxia`, `speech delay`, `autism`, `ASD`) + a passing `outputScreen.test.ts` case. If the prompt bank is 100% static curated strings authored/reviewed by clinical-lead with NO model authorship anywhere in LANG-15, the output-floor is satisfied by construction (CI-30/AP-071 precedent) — document that and the floor is met; the static bank still requires the clinical-lead string review + build-time lint.
- **(B) The "We tried this" log + "This Week" panel stays a parent-attributed descriptive note — must NEVER become a scored/trended child-language metric; + arbor-safety COPPA on the write path.** The design already specifies "no child-language-output metric, NO language-growth chart, NO count toward a milestone, NO accuracy percentage" — hold this **verbatim and binding**. The `lang-strategy` PracticeEvent counts the PARENT's logged moments (same flat-count pattern as all other HeroArcade worlds); it may **never** aggregate into a per-child language score/%, "talking more"/"on track" verdict, or an emotion/word-count/intensity trend chart (the CI-24 §2 + CI-30(B) leak: a trended child metric = implicit expressive-language assessment, the exact slp-scope line). The HeroArcade tile count = parent strategy-moments logged this week (a tally of the parent's actions), never a child metric. The PracticeEvent is a child-data write → **independent `arbor-safety` COPPA review on the write path** (reuse CI-23/CI-24/CI-30 precedent).
- **(C) The referral rail — REUSE the CI-25 string verbatim, never auto-fire, slp-scope numbers source-verified IF any number ever surfaces.** The design honors CI-23 §3: the rail reuses the CI-25 "worth raising with your pediatrician or an SLP" string, is always available, never auto-fired. **Binding narrowing on the exact copy:** the rail line and the share-sheet text must read as a parent-owned conversation-starter (CDC surveillance voice), never an Arbor verdict. The design's share-sheet text "I would like to discuss my child's language development at our next visit" is **clean** (parent-authored, first-person, no condition, no verdict). The rail line "Something feels worth discussing? It is always worth raising with your pediatrician or an SLP." is clean. **slp source-verify rule (CI-23 §3, binding):** LANG-15 as designed puts NO numeric language threshold in copy (no "50 words by 24m," no intelligibility %) — that is the safe path and it is held. IF any future iteration surfaces a numeric milestone to gate or justify the rail, clinical-lead must verify it against the live ASHA/CDC page before it ships (the rail in the *current* design needs no number, so this is a guard, not an open fix).

**3. Secondary holds (clearable in-copy/config) + firewall:**
- **Register firewall (the design's own load-bearing choice — hold it):** parent-only surface, parent-calm register (SectionCard/cardCls/kit.tsx), NEVER PlayShell/PlayKit/Sprout-mascot/confetti, NEVER a child-selected world gate. This keeps it out of the kid-companion class — binding. The "Logged. Great moment." confirmation is clean (two words, no praise-of-the-child's-development, no valence-on-the-child).
- **Module/world naming:** "Word World," "Serve and Return," "Narrated Play," "Shared Reading" are behavior/activity names, no condition-noun, no branded program (no Hanen / *It Takes Two to Talk* / *More Than Words* / OWL / 3A's-method / "dialogic reading" as a branded-curriculum citation) — confirmed clean against CI-23 §2. Build-time lint: branded-program token list across prompts AND any citation/"further reading."
- **The monitoring-nudge + Ask Arbor deep-links (Flows 2/3):** "Try some language moments in Word World" is a routing nudge to an activity, no developmental claim, no verdict — clean. It must surface an observable activity prompt, never a domain-label verdict or "[child] may be delayed" inference (inherits the CI-30 why-line / CI-27 DailyPlayCard rule); if the nudge string is model-composed it inherits the output-floor.
- **Firewall §0:** all copy "developmentally informed, grounded in CDC/AAP/ASHA/WHO," never "clinically validated/clinician-reviewed/assesses/screens/evaluates/SLP-designed," no adviser/thinker identity; mechanism researchers (Hanen evidence base etc.) stay back-end only, never cited in copy (CI-23 §0/§2).

**Lens splits:** **slp (LEAD) — pass-conditional:** parent-mediated serve-and-return / shared-reading is squarely the highest-evidence early-language intervention and a parent narrating their own actions / following the child's lead needs no SLP — so a no-claim coaching surface that teaches the MECHANISM (not a fidelity-gated clinical program) is in-scope and sound; the binding lines are (i) NO child-language-output metric ever (no word-count, intelligibility %, "talking more" verdict — that is the slp expressive-language-assessment leak), (ii) no branded-program name (Hanen/*It Takes Two*/OWL — certification-gated, SLP-delivered; invoking it implies the evidence without the delivery conditions, CI-23 §2), (iii) the persistent-concern path routes to the reused, non-auto-firing CI-25 rail, numbers source-verified live IF ever surfaced. **peds — pass-conditional:** the prompts are age-band-matched (0-12m / 12-36m / 3-5y) which is correct and entirely within CDC LTSAE's 0-5 anchor (no 6-10 overreach here — the band caps at 5, so "grounded in CDC/AAP" is validly usable, unlike the CI-22 6-10 EF surface); the only watch = the "Today's Moment" prompt staying a practice activity, never a flag/verdict about whether the child is behind. **psych — pass, rubric-positive:** routes the parent AT a shared real-world language moment = parent-mediated, attachment-safe, competence-building (hands the parent words to use, builds their attunement), never a kid-companion; the parent-calm register + "no language-growth chart" keeps it out of child-as-project framing; the log is the parent's own action, never a child-progress score.

**screenModelOutput enforcement:** **REQUIRED IF any LANG-15 string is model-/template-authored** (prompts, age-band match text, monitoring nudge). If the prompt bank + all copy are 100% static curated/clinical-lead-reviewed strings with zero model authorship, the floor is satisfied by construction — document the by-construction claim + keep the build-time lint (condition-name + effect-verb + branded-program + child-language-metric bans). Given the design describes a finite prompt bank per module×age-band, the static-bank path is the likely and recommended implementation.

**cited_basis:** CDC "Learn the Signs. Act Early." 2022 (Zubler et al., *Pediatrics* 2022 — 0-5 milestone anchor, surveillance routes parent to provider, no app verdict); AAP surveillance-vs-screening (Lipkin/Macias 2020 — observe/route, never score); ASHA Scope of Practice in SLP + public developmental norms (serve-and-return / dialogic shared reading = replicated parent-mediated early-language *mechanism*; expressive-language *progress* reserved to a qualified professional, software coaches the parent's action, does not assess the child; ASHA communication-milestone page = source of truth, numbers verified live only if surfaced); CHARTER §3 p11 + advisory.md §0 (firewall — provenance not endorsement; Hanen/branded programs certification-gated, back-end only). Reuses CI-23 §1/§2/§3/§4, CI-25, CI-30, CI-27/AP-071 verbatim. **Honesty flag:** the mechanism is well-established but **effect sizes are program-specific and not claimed here** — the design is defensible precisely because it teaches the mechanism and produces no measurement and asserts no effect; the moment any surface logs a child-language metric, implies the activity *works on* the child, or names a language condition, it crosses into the slp-assessment / effect-claim class and flips to VETO.

**Verdict:** soundness:**concerns(clearable)** / claims:**none** / riskClass:**gated → BUILD-READY-NARROWED.** Buildable in this run **only** with: (1) "This Week"/log stays a parent-attributed descriptive moment list — **NO child-language-output metric, no language-growth chart, no milestone count, no accuracy %, no "talking more"/"on track" verdict on the child anywhere** (verbatim from the design — held binding); (2) referral rail reuses the CI-25 string verbatim, never auto-fired, share-sheet text as designed, no numeric language threshold in copy (source-verify live IF ever surfaced); (3) parent-only, parent-calm register — never PlayShell/PlayKit/child-gate/confetti/mascot; (4) no branded-program name (Hanen/*It Takes Two to Talk*/*More Than Words*/OWL/dialogic-as-branded-curriculum) in prompts OR citations; (5) screenModelOutput + CONDITIONS(+language leaks) + test IF any string is model-/template-authored, else document by-construction static bank; (6) effect-verb + comprehension-leak + child-language-metric + branded-program build-time lint; (7) arbor-safety COPPA clears the `lang-strategy` PracticeEvent write path; (8) firewall §0 on all copy. Cleared to `soundness:pass` on a final board copy pass once those are in the actual strings/code. Routed to **arbor-pm** as a targeted finding and through `arbor-orchestrator` as build-ready-narrowed (the narrowing is binding on the build pod, identical handling to an arbor-safety condition). "Non-diagnostic wording" was ruled on substance and the design clears it. **NOTE:** LANG-15 is a CI-23-class capability — its SLP-led framing requirement is satisfied by this gate (slp lead, no-claim mechanism-only version cleared); it is **NOT** a new instrument, not a screener, not an effect-claim surface.

## Council Intake — 2026-06-21 (advisory scores)
| ID | Title | Stream | riskClass | Priority | Clinical | Advisor | Owner pod | Note |
| DEM-005 | Live Expert Session Booking — calendar + video + in-app payment, memory-packet pre-load | demand | gated | n/a | **board: soundness:concerns / claims:UNSUBSTANTIATED — HELD (see Clinical GATE block above)** | tension | arbor-growth | **Tension — the differentiator is rubric-positive, the framing tilts engagement-bait and carries two firewall/safety traps.** The memory-packet pre-load is genuine value: it routes the parent OUT to a real human (**competence**: the off-ramp, not deeper app-reliance) and makes the session richer than cold telehealth (**truth + order** — the provider walks in already knowing the child). Three problems as written. (1) **competence→dependence risk** — a marketplace whose north-star metric is "packet-share AND book = 15% of exports" optimizes *consults booked*, not the parent needing fewer of them; if booking becomes the reflex answer to every worry it trains "the app handles my parenting" over the parent's own judgment + a durable real-world provider relationship. (2) **the firewall trap** — "expert" must NEVER read as "Arbor's clinician"; the rationale's own caveat ("expert identity must never be presented as a licensed clinician via Arbor's branding") *is* the line — the moment booking copy implies Arbor *provides* or *endorses* licensed care it's a clinician-endorsement claim → marketing+safety veto (CHARTER §3 p11). (3) **deepest child-data egress in the product** — full memory packet to a third party; consent must be per-session, scoped, time-boxed, revocable, framed as the parent *granting* access they own (a checkbox is not enough). **Reframe (the competence-building version):** position it as *"bring your own provider into the record," not "book an Arbor expert."* (a) Default the flow to the parent's **existing** professional (invite-your-pediatrician/SLP) before any marketplace — strengthens the real outside relationship instead of substituting an app one. (b) Make the **packet the product, the booking the wrapper** — success metric = *session produced a concrete plan the parent owns / fewer repeat consults over time*, NOT consults-booked or rebooking rate. (c) A "do you still need this?" graduation stance over a subscription-to-consults loop — the loop should reduce dependence, not farm it. (d) Hard copy guardrail: "an independent professional you choose," explicit "Arbor does not provide medical care; these professionals are not employed or endorsed by Arbor," no licensure implied by Arbor's brand. Built that way it climbs toward aligned. Correctly `gated`/`touchesClinical`: billing (Stripe) + third-party child-data egress + provider-identity framing all need Guy + `arbor-clinical-lead`/`arbor-safety` sign-off before build-ready; never auto-built. Firewall: booking + packet copy carry no adviser/thinker identity and no clinician-endorsement claim. confidence 0.6 appropriately hedged. |
|----|-------|--------|-----------|----------|----------|---------|-----------|------|
| PHI-08 | Consent + audit panel as parental ownership of the record | philosophy | gated | 4.8 | n/a | aligned | safety | Hits responsibility-before-comfort dead-on; also serves competence/order/truth. Reframe is a copy sharpening, not a fix: frame as **owning** the record (see/govern/revoke), not as **reassurance** ("you're safe with us"). Keep firewall: consent copy carries no adviser/thinker identity. Gated per riskClass (touches consent) → human decision, not auto-build. |
| PHI-10 | Honest red-flag layer: name hard signals plainly, route to a provider, never soothe by hiding | philosophy | gated | n/a | needs board pass | aligned | safety | Highest-alignment philosophy candidate scored: this **is** the truth-before-avoidance test, and it scores strong on all five lenses (competence: routes outward to a real provider, not app-reliance; order: turns worry into a named, actionable signal; responsibility: nudges agency over reassurance; meaning: serves development at the explicit cost of engagement). Advisory = weighting only, NOT a green light. Two execution guardrails the council must hold: (1) **surveillance-not-diagnosis copy is the whole feature** — "a signal to discuss with your provider," never a label/verdict; if it hardens into pseudo-diagnosis it flips truth→false-alarm and pathologizes normal variation (clinical veto). (2) **Firewall:** red-flag copy + provider PDF carry no adviser/thinker identity and no clinician-endorsement claim — "developmentally informed, grounded in CDC/AAP/ASHA," never "clinically validated." Correctly `gated`/`touchesClinical`: hands `arbor-clinical-lead` a veto on ASQ-style thresholds + every word of claim/escalation copy. Cannot be build-ready until soundness:pass + claims:substantiated; confidence 0.6 appropriately hedged → human decision. |
| CLI-03 | Surface CDC "Act Early" red-flags non-alarmistly (worth-discussing-soon, never a diagnosis) | clinical | gated | n/a | needs board pass | aligned | arbor-ai | **Truth-before-avoidance, the time-sensitive case.** Today `deriveMonitoring` (`monitoring.ts`) only catches *past-band* milestones (with a 2-month grace) + behavior-pattern clusters — both of which are slow, retrospective signals. The CDC hard-stop set (loss of skills, no words by ~16m, no joint attention/gestures, not walking by ~18m) is the one place where *waiting for the next band* is the wrong move and silence soothes by hiding. Scores strong on all five: **truth** (names the hard, time-sensitive signal instead of letting it pass quietly), **responsibility** (a calm "worth discussing *soon*" nudges agency, not reassurance-seeking), **competence** (routes the parent outward to a real provider on a clock, not deeper into the app), **order** (turns a vague worry into a specific, dated, actionable prompt), **meaning** (serves the child's development at the explicit cost of a "nothing's wrong" feel-good moment). **No reframe needed — already the competence-building version**; the only refinement is *tone-of-urgency without alarm*: "worth a conversation in the next few weeks," never "regression detected"/"red flag." Two guardrails the council must hold: (1) **the never-a-label rule is the whole feature** — surface the missing *behavior* ("no single words noted by ~16 months"), never the *condition* it screens for; the existing module's "on-track vs worth-discussing, always ends in a provider nudge" framing must extend verbatim to the hard-stop set or it flips truth→false-alarm and pathologizes normal variation (clinical veto). (2) **Firewall:** all red-flag + urgency copy is "developmentally informed, grounded in CDC 'Learn the Signs. Act Early.'", never "clinically validated," and carries no adviser/thinker identity. Correctly `gated`/`touchesClinical`: `arbor-clinical-lead`/`-peds` must return soundness:pass on the exact CDC thresholds, the age windows, the grace/urgency wording, and every word of escalation copy before build-ready. High reach×impact, moderate effort, real safety value; confidence 0.8 reasonable. Pairs with CLI-01 — corrected-age must land first so a preterm infant isn't urgency-flagged on raw age. |
| CLI-04 | SLP referral-timing prompt in Practice Studio tied to ASHA intelligibility windows | clinical | gated | n/a | needs board pass | aligned | arbor-practice | **Truth-before-avoidance on the speech surface** — the speech-twin of CLI-01/CLI-03/PHI-10. `speechScorer.ts` today measures accuracy and stays silent, which is a comfort tool: a persistently low intelligibility trend slides by un-named. Tying it to ASHA windows (~50% understandable by 2y, ~75% by 3y) as a gentle "consider a speech-language assessment" scores 5/5: **competence** (routes the parent *out* to a real SLP, not app-reliance), **order** (turns raw accuracy numbers into a named, actionable threshold), **responsibility** (nudges agency over a soothing silence), **truth** (names a hard signal honestly), **meaning** (serves development at the explicit cost of the daily-practice engagement loop — the right trade). **No reframe needed — already the competence-building version.** One sharpening (copy, not fix): frame as *a signal to discuss with your provider*, never Arbor interpreting/labeling the child — surveillance-not-diagnosis is the whole feature; if it hardens into a delay verdict it flips truth→false-alarm and pathologizes normal variation (clinical veto). Correctly `gated`/`touchesClinical`: the ASHA windows, the "persistently low" threshold + grace window, and every word of referral copy are clinical thresholds — `arbor-clinical-lead` (`arbor-clinical-slp`) must return soundness:pass before build-ready. Firewall: copy is "developmentally informed, grounded in ASHA," never "clinically validated," and carries no adviser/thinker identity. The rationale's "no effect-size/benefit claim" is the right instinct — hold it. |
| CLI-05 | Escalation/red-flag copy clinical re-review + jurisdiction-currency hook | clinical | gated | n/a | needs board pass | aligned | arbor-safety | **Truth-before-avoidance, the maintenance case — and the highest-stakes one.** `escalation.ts` is already the competence-building version: sound, trilingual (EN/HE/NL), leads with 112/101/911 + national lines + findahelpline, and its whole design routes the parent *outward* ("pause the Arbor plan… get another adult", §3/§4 of `renderEscalationMarkdown`) — never deeper into the app. Scores strong on all five: **competence** (the feature *is* the off-ramp to a real person/emergency service), **truth** (a stale or wrong emergency number is the app appearing to help while silently failing — the exact "soothe by hiding" the rubric exists to kill), **order** (a dated periodic re-review replaces "hope the hard-coded numbers are still right" with an owned maintenance rhythm), **responsibility** (hands agency to a qualified human at the crisis moment), **meaning** (zero engagement angle, pure safety value). The maintenance complement to PHI-10/CLI-03/CLI-04: those add the honest signal, this keeps the routing they depend on alive. **No reframe needed — already competence-building**; one guardrail the council must hold: the re-review must **verify each pass against a live authoritative source** (national emergency/helpline registries), not merely re-confirm the existing literals, or it degrades into a rubber-stamp that re-blesses a rotted number. Correctly `gated`/`touchesClinical`: escalation/helpline copy is precisely what `arbor-clinical-lead`/`arbor-safety` must sign off — soundness:pass on every number, jurisdiction, and word of crisis copy before build-ready. Firewall holds (numbers are emoji-flagged jurisdictions only, no person; any copy stays "developmentally informed", never "clinically validated", no adviser/thinker identity). High reach×impact, trivial effort (1), confidence 0.9 reasonable. |
| CLI-07 | Corrected-age capture + display in onboarding/profile (AAP 24-month ceiling) | clinical | gated | n/a | needs board pass | aligned | arbor-growth | **Truth-before-avoidance, at the input.** Verified: `types.ts` defines `preterm.gestationalWeeks` and corrected-age logic exists in `childAge.ts`/`milestoneData.ts`, but the *capture/display* UX is thin — so the honest baseline is computable yet rarely entered. Gently prompting gestation, showing a "corrected age" badge, and stopping correction at 24 months corrected (AAP) is the data prerequisite that lets CLI-01 (corrected-age in monitoring) and CLI-03 (CDC red-flags) fire on a *true* age rather than a raw one — without it those two false-alarm a preterm infant. Scores clean: **truth** (measures the child against the honest baseline), **responsibility/competence** (parent acts on a true signal; the bounded 24-month ceiling is an honest boundary, not a forever-asterisk), **order** (a named correction rule replaces ambiguity); no engagement dimension. **No reframe needed — already the competence-building version.** Guardrails: (1) the `(40 − gestationalWeeks)` math + the 24-month ceiling + the gestation-prompt copy are clinical thresholds — `arbor-clinical-lead`/`-peds` must return soundness:pass before build-ready; (2) firewall — corrected-age UI copy is "developmentally informed, grounded in AAP," never "clinically validated," no adviser/thinker identity. Correctly `gated`/`touchesClinical` (child-data capture + a developmental-correction rule) → human decision, not auto-build. Highest leverage of the clinical cluster: low effort, unblocks two already-scored items; confidence 0.85 reasonable. |
| CLI-01 | Wire preterm corrected-age into the live developmental-monitoring path | clinical | gated | n/a | needs board pass | aligned | arbor-ai | **Textbook truth-before-avoidance** — the live path currently lies by omission: `correctedAge`/`comparisonAgeMonths` exist in `milestoneData.ts` (L316/L339) but `deriveMonitoring` in `monitoring.ts` derives `ageMonths` from raw `input.ageYears` (L181) and never calls them, so a preterm infant is flagged "overdue" early against AAP corrected-age guidance — a false alarm the rubric exists to kill. Also serves order-from-chaos (an honest baseline beats a noisy one) and responsibility (the parent acts on a *true* signal, not an artifact). High reach×impact, low effort, a real correctness bug — strong build candidate. **No reframe needed** (it is already the competence-building version). Correctly `gated`/`touchesClinical`: the corrected-age formula `(40 − gestationalWeeks)` and the overdue threshold are clinical thresholds — `arbor-clinical-lead` (`arbor-clinical-peds`) must return soundness:pass on the math + grace window before build-ready. Firewall: any new preterm UI copy is "developmentally informed, grounded in AAP," never "clinically validated," and carries no adviser/thinker identity. |
| DEM-001 | FCM background push for JITAI predictive nudges | demand | gated | n/a | n/a | tension | arbor-growth | **Tension — the capability is rubric-positive, the framing is engagement-bait.** The JITAI moment (an optimal-time prompt computed from the child's *own* append-only rhythm record) is real value and serves order-from-chaos; the problem is the *delivery framing*. As written it's reach-first — "a reach advantage no single-vertical competitor can replicate," owned by arbor-growth — i.e. background push as a re-engagement lever (matching Huckleberry SweetSpot / Kinedu live-class push). That tilts three lenses the wrong way: **competence→dependence** (trains the parent to wait for the app to tell them when to act, instead of building their own attunement to the child), **responsibility→comfort** (interruptive push is the classic outsourcing/reassurance channel), **meaning→engagement** (the stated win is reach/sessions, the exact pattern the rubric down-weights). Push is also the noisiest surface in the product — the order-from-chaos upside flips to noise if it fires too often. **Reframe (the competence-building version):** ship the nudge as a *low-frequency, parent-governed rhythm cue, not a re-engagement push.* (1) Frame the payload as handing the parent a window they own — "good window for [child]'s wind-down coming up" — never "open Arbor"; the cue teaches the rhythm, not the app. (2) Parent sets cap + quiet hours at the consent opt-in (default ≤1/day, snoozable, easy full-off); success metric = *acted-on-without-opening-the-app* / rhythm-habit-formed, NOT push-driven sessions. (3) Add a graduation path: as the parent's own timing stabilises in the record, the cue *backs off* — the loop's goal is to make itself unnecessary. That converts "reach advantage" into "we helped you build the habit, then got out of the way." Correctly `gated` (FCM consent + server-side child-data read) → never auto-built, human decision. `touchesClinical:false` is right (no developmental claim, so no clinical-board block); firewall still applies — nudge copy carries no adviser/thinker identity. |
| DEM-002 | Proactive "Arbor Noticed" Weekly Alert Card | demand | gated | n/a | needs board pass | tension | arbor-growth | **Tension — the right substance, the wrong frame** (the digest sibling of DEM-001's push case). The *mechanism* — a longitudinal weekly read grounded in the child's *own* append-only record, surfacing `monitoring.ts` WatchLevel signals that today only live on the Screening tab — is genuinely competence/order/truth-building and is the real moat. But the rationale leads with "highest-moat-leverage **retention** bet" on a **push-initiated** card benchmarked to Good Inside (Gigi) / Huckleberry proactive tips — that optimizes time-in-app. That fails **meaning-over-engagement** (off-rubric as framed) and tilts **competence→dependence** (a weekly push pulls the parent back in / trains reliance on the app to notice for them) and **responsibility→comfort** (a card pushed *at* the parent is reassurance-delivery). Two of five aligned (**order**: one weekly grounded read beats scattered signals — *if* it stays weekly and bounded, not a notification stream; **truth**: conditional — only if it names the honest signal, including a *worth-discussing* one). **Reframe — turn the retention bet into a competence loop:** (1) a card the parent **opens**, a calm weekly digest, never a push that pulls them back; success metric = *a parent action taken / a real conversation had*, NOT opens / DAU / sessions. (2) Every card **ends in one parent-owned next step** (observe X this week / a question to ask / when to involve a provider), not an in-app re-engagement. (3) **Truth guardrail is load-bearing** — surfaces the honest signal including the worth-discussing one, never a curated "everything's lovely" highlight reel tuned for retention, which would flip truth→avoidance. Built that way it climbs to aligned. Correctly `gated`/`touchesClinical`: WatchLevel thresholds + every word of the non-diagnostic card copy need `arbor-clinical-lead` soundness:pass before build-ready — "a signal to discuss with your provider," never a label/verdict. Firewall: copy is "developmentally informed, grounded in CDC/AAP/ASHA," never "clinically validated," no adviser/thinker identity. Carries the honest-signal substance of CLI-03/PHI-10; depends on CLI-01/CLI-02/CLI-07 landing first so the longitudinal read isn't built on uncorrected-age / unwindowed-score artifacts. reach×impact strong, confidence 0.8 reasonable. |
| DEM-004 | Expert-Cited Activity Content — citation fields on PlayActivity | demand | gated | n/a | needs board pass | aligned | arbor-practice | **Aligned — competence + truth, with the claim correctly fenced.** A real citation routes the parent *outward* to the source authority (CDC/AAP/named researcher) instead of deeper into the app, and arms agency rather than reassurance-seeking (**competence + responsibility**); concern-matched sourcing turns a generic feed into an ordered, relevant signal (**order**); no streak/dopamine mechanic — it serves trust, not time-in-app (**meaning**). The **schema change is safe**; the load-bearing test is **truth-before-avoidance** in the *populated copy* — a paper cited next to an activity reads as "clinically proven to help your child's [concern]," exactly the developmental/effect-size claim the rationale already files `gated`/`touchesClinical`. **No reframe — already the competence-building version**; one sharpening (copy, not fix): make the citation a *verifiable door, not a credibility badge* — surface source + scope + a working link the parent can actually open ("grounded in CDC 'Learn the Signs'", "from a 2021 study on X"), never a bare authority halo or an implied effect-size; the personalization must point at evidence *for the concern*, never assert the *activity treats it*. Correctly `gated`/`touchesClinical`: `arbor-clinical-lead` (+`-peds`/`-slp` per domain) must return soundness:pass + claims:none|substantiated on every populated citation before build-ready. Firewall: citation copy is "developmentally informed, grounded in CDC/AAP/ASHA," never "clinically validated"/"clinician-approved," and carries no adviser/thinker identity. High reach×impact (4×4), low effort (2), confidence 0.9 reasonable — strong build candidate once copy clears the board; split the work so the safe schema can land while populated copy stays gated. |
| DEM-003 | Physical Growth Tracking — Height/Weight/WHO percentile (append-only profile log + timeline) | demand | gated | n/a | needs board pass | tension | arbor-growth | **Tension — strong on order/meaning, but the percentile-as-score framing trips responsibility + truth.** The append-only, parent-owned growth record is genuinely worth building: it serves **order-from-chaos** (scattered visit slips → one longitudinal trajectory), **meaning-over-engagement** (a slow, sparse log, not a streak — low dopamine risk), and compounds the longitudinal-record moat the council breaks ties toward. Where it slips: a raw WHO **percentile surfaced as a number/rank** invites **reassurance-seeking** (responsibility test — "90th, we're winning" / "12th, panic") and is a **half-truth** (truth test) because a single reading is noise; only a *trend* is signal. It also carries the rationale's own flagged **implied-medical-interpretation** risk, and tilts **competence→dependence** if it becomes a daily "is my kid winning" check rather than a parent learning to read their child's own curve. **Reframe (framing fix, not a rejection — the highest-value move here):** log height/weight and plot the child's **own curve over time**; show the percentile as *quiet context for the trajectory*, never as a headline score or verdict. Surface only the **honest, actionable** signal — a *crossing-percentiles* / trend-break pattern as "worth raising with your provider," never "underweight"/"falling behind" (that flips truth→false-alarm and pathologizes normal variation — clinical veto). That makes it the competence-building version: the parent learns their child's own trajectory and routes *outward* to the pediatrician on a real signal, instead of self-soothing or self-diagnosing on a single number in-app. Correctly `gated`/`touchesClinical`: the WHO chart math, percentile bands, and the trend-break/"worth discussing" threshold + all copy are clinical thresholds — `arbor-clinical-lead`/`-peds` must return soundness:pass and `arbor-safety` clear the child-health-data (COPPA/GDPR) schema before build-ready; pairs with CLI-07 — corrected-age must land first so a preterm infant isn't plotted on raw age. Firewall: growth/percentile copy is "developmentally informed, grounded in WHO growth standards," never "clinically validated," and carries no adviser/thinker identity. Advisory = weighting only (tension down-weights priority); not a green light, not a block. reach 4 / impact 3 / confidence 0.8 reasonable. |
| DEM-007 | Referral Grant + /join?ref= Deep-Link Resolver | demand | gated | n/a | n/a | tension | arbor-growth | **Tension — sound plumbing, growth-bait framing.** Four of five lenses neutral, one negative: the `/join?ref=` resolver + `invite_sent`/`invite_activated` instrumentation are legitimate, rubric-neutral infrastructure, but the rationale is explicitly K-factor / "viral ignition" / "incentive to share" — a paid-month bounty that rewards the *act of recruiting a stranger*, the exact time-in-app/engagement mechanic **meaning-over-engagement** down-weights (and the council's strategic_fit multiplier exists to discount). It builds *distribution*, not the family's developmental **competence**, and the bounty trains share-for-reward, not anything done for the child. **Reframe — turn the referral bounty into the second-guardian loop:** keep the resolver + event instrumentation (neutral plumbing, ship them), but fire the grant on *a real caregiver joining **this child's** care circle and contributing an observation* (co-parent/grandparent/caregiver), not on raw invite volume. Success metric = **circles that became real** (a second adult actively contributing to the record), NOT invites_sent / K-factor. That converts "share to get a free month" into "bring the people who actually care for your child onto the record" — which deepens the longitudinal record (the moat) and the parent-mediated model, and is a stronger retention story than a generic referral. Voice not veto: stays correctly `gated` (writes entitlements/billing) — Guy confirms reward amount + that billing rails are live before ship. Firewall: referral copy is user-facing → carries no adviser/thinker identity (the reframe preserves this). `touchesClinical:false` correct (no developmental claim). reach×impact strong (5×5), effort 3, confidence 0.95 — but score the *reframed* version; the as-written K-factor framing earns the strategic_fit down-weight. |
| DEM-008 | One-Tap Branded Share Export with Referral Code in Deep Link | demand | gated | n/a | n/a | tension | arbor-avatar | **Tension — the export pipeline is rubric-neutral plumbing, the framing is acquisition-bait** (the share-surface sibling of DEM-007's resolver; this is the artifact + code-injection layer that rides on it). A one-tap branded export is a legitimate capability — the Growth Card / Hero Comic already travel via WhatsApp, and product owns the render/share-sheet/code-injection. The problem is the *payload thesis*: the rationale leads with "the loop's **conversion point**," reach 5 / impact 5, with a **billing entitlement grant** firing on recipient activation. That makes the embedded **referral code** the point, not what the artifact says about the child — the exact **meaning-over-engagement** pattern the rubric down-weights (off-rubric on that lens as framed), and it tilts **competence→dependence** (the parent's job becomes recruiting installs, not building attunement to their child). Two of five clear: **truth** holds *only* because the rationale already fences it right (stylized HeroAvatar only — no real face, no raw developmental data; this fence is load-bearing and must not erode); **order/responsibility** are neutral. **Reframe (the competence-building version, the highest-value move): make the thing that travels carry value, not just a code.** (1) Lead with the *artifact* and append the referral as a quiet footer — a parent shares because they're *proud of their kid*, never to farm a discount; share copy is about the child's progress, not "invite friends, get a month free." (2) Success metric = *artifacts shared because they were meaningful* (and the recipient getting a genuinely useful first read), NOT referral-activations / viral-K. (3) Keep the entitlement grant **out** of the philosophy framing — it's a separate billing mechanic (DEM-007); the share feature should not be *designed around* the payout. Built that way the export is a neutral-to-mild-positive distribution primitive instead of a recruitment lever, and it climbs out of tension. Correctly `gated` (the rationale's own `claim`: entitlement-grant = **billing**, plus deep-link + render of child-derived content) → never auto-built, human decision. `touchesClinical:false` is right (stylized avatar, no developmental data leaves the device → no claim, no clinical-board block) **provided the no-real-face / no-raw-data fence is enforced in the render pipeline, not merely asserted** — that fence is the whole safety surface. Firewall: branded export + all share copy carry no adviser/thinker identity and make no developmental/effect-size claim about the child in the shared artifact. Advisory = weighting only (tension down-weights priority); not a green light, not a block. |
| DEM-009 | Double-Aha Onboarding — Comic→Coach Answer→Daily Play→Rhythm Promise (<60s) | demand | gated | n/a | needs board pass | tension | arbor-design | **Tension — right substance, engagement-bait frame.** Showing a real personalized coach answer + concern-tied Daily Play + Rhythm read in the first session demonstrates the *actual moat* (the child record), not a generic tour — that serves **order** (parent leaves with structure) and **truth** (the aha is grounded in *their* child). But the framing is an **activation gate for the viral sprint** ("40% activated," "converts at near-zero," "<60s before paywall"), which tilts three lenses wrong: **meaning→engagement** (the stated win is funnel conversion, off-rubric as framed), **competence→dependence** (a slick <60s reveal teaches "the app dazzles me," not "I understand my child"), **responsibility→comfort** (a reveal delivered *at* the parent is reassurance, not agency). The **Rhythm "promise"** at first-run — before any of the child's own record exists — risks an unearned prediction (truth hazard). **Reframe — turn the activation gate into a first *competence loop*:** (1) the onboarding coach answer is a genuine answer to the concern the parent just typed, ending in **one parent-owned next step**, never a teaser that resolves only after the paywall; (2) replace the Rhythm *promise* with an honest Rhythm *invitation* ("log the next few days and Arbor starts seeing [child]'s pattern") so the prediction is *earned by the record*, not asserted day-zero; (3) success metric = **first-action-completed** (activity tried / question asked / first data point logged), NOT activated/converted-in-<60s — speed becomes a fluency constraint, not the goal. Built that way "parent saw the moat" and "parent took the first competent action" are the same moment → climbs to aligned. Correctly `gated`/`touchesClinical`: the onboarding coach answer carries implicit developmental framing — `arbor-clinical-lead` + `arbor-safety` must clear every word (soundness:pass, claims:none) before build-ready; no effect-size/diagnostic implication in first-run. Firewall: "developmentally informed, grounded in CDC/AAP/ASHA," never "clinically validated," no adviser/thinker identity. reach×impact 5×5 strong, effort 3, confidence 0.8 — high-leverage *once the frame is competence-first and copy clears the board*. |
| DEM-010 | Founding-Member Paywall Trigger — €89/yr at Post-Aha Moment with 500-Slot Cap | demand | gated | n/a | n/a | tension | arbor-billing | **Tension — charging at proven value is fine; the manufactured-scarcity framing is the problem.** Charging is rubric-neutral-to-positive: an honest annual subscription that fires *after* the parent has actually seen value (the Double-Aha), with a no-card trial as the default path, is *less* extractive than streaks/ads and broadly serves **meaning-over-engagement** (no dopamine loop, value-first). Two lenses tilt negative as written: (1) **truth-before-avoidance** — a "500-slot cap" is fake scarcity unless it is a *real, honored* constraint; an app stating an untrue limit to convert is precisely the "sell by hiding the truth" pattern the rubric exists to kill. (2) **responsibility-before-comfort** — firing at "peak value recognition" + countdown scarcity engineers a reactive FOMO reflex rather than the parent's considered decision. **Reframe (framing fix, not a rejection):** (a) make the founding cohort a *real* promise — either a genuinely capped, honored cohort with a durable founding benefit (locked price / early-access channel), or drop the slot mechanic entirely; never display a fake counter. (b) Keep the trigger at proven-value but frame the offer as a *choice the parent owns*, not a closing-now urgency play — "you've seen what Arbor remembers; here's the founding price" with the no-card trial always visibly available, not buried behind the scarcity variant. (c) Instrument success as *informed conversion that retains* (trial→paid that doesn't churn at renewal), NOT slot-pressure conversion rate. Built that way it climbs to aligned. Advisory = weighting only (tension down-weights), not a block. Correctly `gated` (Level 4 billing + entitlement) — Guy confirms price, the cap's *reality*, and the kill-gate before ship; the kill-gate (no live paywall until 50 organic reviews OR press) and the no-card-trial default are both rubric-positive instincts — hold them. `touchesClinical:false` correct (no developmental/medical claim → no clinical-board block). Firewall: paywall copy is user-facing → carries no adviser/thinker identity (the reframe preserves this). reach×impact strong (5×5), effort 3, confidence 0.8 — but score the *reframed*, real-scarcity version; the fake-cap variant earns the strategic_fit down-weight. |
| DEM-011 | Email / Waitlist Capture with GDPR Consent on Landing | demand | gated | n/a | n/a | tension | arbor-growth | **Tension — sound, compliant plumbing wrapped in lead-capture framing.** The consent *mechanism* itself is rubric-neutral-to-good and genuinely needed: explicit opt-in, a real privacy notice, no pre-tick, a wired unsubscribe path hand the visitor honest control (**responsibility/truth** — no dark patterns), and on an EU/IL child product it is a true GDPR prerequisite (capturing PII without it *creates* the liability, so this gate must precede any list). Where it slips is the **stated purpose**: "interested-but-not-ready visitors are *permanently lost*" + "email nurture as a top-of-funnel *retention lever*", benchmarked to Good Inside/Kinedu, is capture-the-lead / drip-them-back framing — the exact engagement-funnel logic **meaning-over-engagement** down-weights, and it tilts **competence→dependence** the moment it feeds a "come back to the app" nurture sequence (P1-12) rather than building anything for the family. Four lenses neutral, one negative as framed. **Reframe (framing fix, not rejection — the high-value move):** keep the compliant double-opt-in + unsubscribe (ship it; it is the real prerequisite), but make the captured promise a *gift of competence, not a hook*: the not-yet-ready parent opts in to receive **one genuinely useful, source-grounded developmental thing** (e.g. "the 3 things worth watching at your child's age, grounded in CDC/AAP" — a real artifact they can act on *without* an account), and the sequence's success metric is *a parent who felt helped / took one real step*, NOT opens / click-back / re-engagement sessions. Frame the opt-in as "get something useful now," never "don't miss out"; let the value, not FOMO, earn the address. That converts a leak-plugging retention lever into a first proof that Arbor helps *before* it asks — a stronger trust-led GTM, on-brand with the parent-mediated model. Voice not veto: correctly `gated` (PII capture on an EU child product) — `arbor-safety` must review the consent mechanism, lawful basis, retention, and the wired unsubscribe before any address is stored; do not ship without a compliant opt-in. Firewall: all capture + nurture copy is user-facing → carries no adviser/thinker identity, and any developmental content in the welcome gift stays "developmentally informed, grounded in CDC/AAP/ASHA," never "clinically validated" (if the gift makes a developmental claim it draws a clinical-board pass too). `touchesClinical:false` correct as pure plumbing; becomes true if the welcome artifact carries developmental copy. reach 4 / impact 3 / confidence 0.8 reasonable — but score the *reframed* version; the as-written "permanently lost / retention lever" framing earns the strategic_fit down-weight. |
| DEM-012 | Memory-Adaptive Literacy Track for 3-8 Cohort (EarlyReadingTrack wired to child record) | demand | gated | n/a | needs board pass | tension | arbor-practice | **Tension — the memory-adaptive core is the strongest competence story in the demand stream; the "readiness signals to the parent" framing is where it slips.** What's genuinely rubric-positive: wiring `EarlyReadingTrack`/`literacy.ts` to the child's logged concern domains and adapting to how *this* child responded last week is **competence-not-dependence at its best** — the app builds toward the child's actual capability instead of marching everyone through 700 generic phonics lessons, and it does it on the one thing free Duolingo ABC / Khan Kids structurally *cannot* (the longitudinal record = the moat doing real work). It also serves **order-from-chaos** (an adaptive track is a structured path, not a content firehose) and **meaning-over-engagement** *if* the success metric is the child reading, not lessons-completed/streaks. Where it tilts: (1) **truth/responsibility hazard in the word "readiness."** A "readiness signal surfaced to the parent" carries implicit developmental-assessment framing — the rationale flags this itself. Surfaced as a verdict ("your child is/isn't ready to read") it flips **truth→false-alarm** and pathologizes the enormous normal variation in literacy onset (a 5yo not blending yet is *normal*, not behind), and it invites **reassurance-seeking** ("are we ahead?") over agency. (2) **competence→dependence drift on the metric** — an adaptive engine whose north star is time-in-track trains "the app teaches my kid to read" over the parent reading *with* the child (shared book-reading is the highest-evidence literacy intervention; the app must point AT it, not replace it). **Reframe (the competence-building version):** (a) frame the signal as **parent-logged observation, never a screen** — "you've noted [child] is rhyming and recognizing letters; here's where to lean in," surfacing the *behaviors the parent observed*, never a readiness score/verdict/rank; phonological readiness is a *gradient the parent learns to read in their own child*, not a pass/fail Arbor issues. (b) Make the track's success metric **the child's own progression on their own curve + a real-world reading habit** (books read together, letters recognized), NOT lessons-completed / minutes-in-track / streak. (c) Every adaptive step **routes outward to the shared activity** — "try this with [child] tonight" — so the engine builds the parent-child reading loop, not app-reliance; if a persistent, age-windowed plateau shows (ASHA/literacy-milestone windows), surface it as "worth raising with your provider," never "delayed." Built that way it's the clearest **aligned** in the demand stream — the memory moat turned into genuine child competence. Correctly `gated`/`touchesClinical`: every word of readiness/plateau copy + the age windows + the "ready" thresholds carry implicit developmental-assessment framing — `arbor-clinical-lead` (+`arbor-clinical-slp` on phonological/literacy norms) must return soundness:pass + claims:none|substantiated, and `arbor-safety` must clear the parent-facing language, before build-ready; never auto-built. Firewall: all literacy/readiness copy is "developmentally informed, grounded in CDC/AAP/ASHA," never "clinically validated"/"reading-assessment," and carries no adviser/thinker identity. Advisory = weighting only (tension down-weights priority); not a green light, not a block. reach 4 / impact 4 / confidence 0.6 appropriately hedged — score the *reframed* (observation-not-screen, child-progress-not-lessons) version. |
| CLI-06 | Non-pathologizing framing guard on behavior/emotion coaching | clinical | **safe** (board: soundness:concerns / claims:none — NOT gated; no developmental/medical/effect-size claim, no veto) | aligned | arbor-growth | **Sound and rubric-aligned — the rare candidate that *is* the rubric — but it must extend past static copy to the AI-generated strings, and that part isn't built yet.** Verified on the surface: `PatternInsights.tsx` already describes observable behavior only (toughest place/day/time, avg intensity /5, resolve-rate) and infers nothing — clean. The real label-leak risk lives where the model free-writes, and the candidate's reframe names it exactly: `BehaviorsTab.tsx` renders three model-authored surfaces — `/analyze-behavior` (`effectivenessRating`/`expertInsights[].text+heading`/`actionPlanSuggestion`, api.ts L1396-1462), the inline co-regulation script, and the voice-parse (enum-constrained, low risk). Prompt-side the guard already exists: `NON_DIAGNOSTIC_CONTRACT` (contracts/coach.ts) explicitly says "state observations, not labels. Never diagnose autism, ADHD, anxiety, speech delay, trauma" and is applied to `/analyze-behavior` + the inline script; `/analyze-behavior` also runs `screenForImmediateEscalation` first (non-alarmist, routes out). **The gap is the output floor:** `screenModelOutput` (safety/outputScreen.ts) — a lexical diagnosis-label screen whose `CONDITIONS` regex covers autism/ADHD/anxiety/apraxia/etc. — is wired only into the coach + council endpoints (api.ts L352/L481), **not** into `/analyze-behavior` or the inline co-regulation route. So the candidate's load-bearing requirement (bind the non-pathologizing constraint to *any AI-generated insight string on this surface, via prompt PLUS an output screen/test*) is a real, buildable hardening, not redundant. **Required fixes (binding before build-ready):** (1) wrap `/analyze-behavior` and the inline co-regulation route in `screenModelOutput` (reuse the existing function — screen the concatenated `expertInsights`/`actionPlanSuggestion`/`effectivenessRating` + the script body; replace flagged output with the safe fallback); (2) add an output-screen test asserting a synthetic insight string containing an inferred label (e.g. "this looks like ADHD") is caught on these routes (extend `safety/outputScreen.test.ts`); (3) hold the firewall — insight copy stays "developmentally informed," never "clinically validated," and carries no adviser/thinker identity. Soundness:concerns = sound intent, aligned with cited guidance, but ships only after those fixes. **riskClass corrected `gated`→`safe`:** per PRODUCT-COUNCIL §4 `gated` = carries an unverified developmental/medical/effect-size claim or touches consent/billing/cost/child-data; this candidate carries *none* — it is a claim-*suppression* guard with no veto and no unsubstantiated claim, so it does not meet the gating trigger. It still passes the normal DevSecOps gate; nothing here holds it as a clinical block. **Cited basis:** CDC "Learn the Signs. Act Early." (2022 milestone revision) — milestones are for surveillance + a parent-prompted provider conversation, not parent self-diagnosis; AAP developmental-surveillance guidance — observe and refer, never label without formal evaluation; ASHA scope-of-practice — diagnosis of communication/developmental disorders requires a qualified professional, not software. reach 4 / impact 4 / effort 2 / confidence 0.75 reasonable. |
| CLI-02 | Age-window the Development Score to the age-appropriate milestone set | clinical | gated | **board: soundness:pass / claims:none — HELD on peds boundary sign-off** | aligned | arbor-growth | **Aligned — truth before avoidance.** Code confirms the gap: `devScore.ts` docstring promises an "age-appropriate milestone set" but `computeDevScore` scores whatever array it's handed, and `DevScoreCard.tsx:39` passes the full milestone library unfiltered — so a young child reads false-low and an older child reads noisy, then that distorted `focusDomain` (devScore.ts L99-103) drives the parent to the coach with the *wrong* "nurture next." Fixing it serves **truth** (an honest read of where the child *is*, not an app artifact), **competence** (the parent acts on a real signal, not manufactured worry/reassurance), and **order** (a clean per-domain picture instead of noise). **No reframe needed** — it's a correctness fix that strengthens the rubric, not engagement-bait; only guardrail is to preserve the non-diagnostic stance ("conversation starter, not an ability verdict") in the windowed copy. Correctly `gated`/`touchesClinical`: the age→milestone-set mapping (and any preterm-corrected boundary, see CLI-01) is a dev-score-validity claim — `arbor-clinical-peds` must return soundness:pass on the window boundaries before build-ready. Firewall: no adviser/thinker identity, no "clinically validated" — "developmentally informed." |

## Council Intake — 2026-06-23 (advisory scores) — "Functional Development Intelligence" layer (FDI-01..10)

**`arbor-advisor` worth-building rubric on the 10-capability non-diagnostic "functional development intelligence" layer** (parents + gan teachers log functioning → functioning map, Adult Mediation Index, social-comm read, sensory/emotional trigger map, intervention planner, before/after, parent–teacher shared view, committee-ready report to justify a סייעת/support-hours eligibility ועדה). Advisory = weighting + reframe only, NOT a green light. **Where my voice coincides with a likely clinical/safety VETO I say so explicitly** — the council must not read a "tension" weighting as clearance past the board. This input is sharper than DEM-005/CLI-cluster because the *output is a quantified deficit report whose purpose is to document how much help a child needs* — the support-routing core is deeply competence/responsibility-aligned; the same artifact optimized to maximize documented deficit for resource-capture is the inversion.

| ID | Title | Stream | riskClass | Priority | Clinical | Advisor | Owner pod | Note |
|----|-------|--------|-----------|----------|----------|---------|-----------|------|
| FDI-01 | Functional Profile (parent/teacher functioning log across domains) | product | gated | n/a | needs board pass — **likely soundness:concerns**; an instrument-mapped (ABAS/Vineland/SRS/Sensory Profile) per-child functioning profile is the v2 assessment-engine class the board already VETOED — clearable ONLY as a descriptive parent/teacher observation log, never an instrument-mapped score | tension | arbor-growth | **Tension — the moat-grade observation record is rubric-positive; the instrument-mapping is the trap.** A longitudinal log of *what the parent/teacher observed the child do* across transitions/participation/social-comm serves order (scattered worry → one structured picture) and can serve competence (a real picture routes to real help). But "turns ABAS/Vineland/SRS into Arbor capabilities" is exactly the instrument-frame the CI-22/62/CI-24 guardrails ban (a copyrighted norm-referenced instrument's items mapped onto a child = an implicit screen → MDR Rule 11 device-adjacent + label-leak). **Reframe:** a descriptive, parent/teacher-observed *functioning log* ("what you saw [child] do at drop-off"), child-as-own-baseline, never an instrument item-bank, never a normed score/profile-noun. Built as a log it climbs toward aligned; built as an "ABAS profile" it is the vetoed engine. Firewall: "developmentally informed," never "assesses/screens"; no adviser/thinker identity. **My voice coincides with a likely clinical VETO on the instrument-mapping** — weighting ≠ clearance. |
| FDI-02 | Transition Tracker (drop-off / activity-switch / arrival) | product | gated | n/a | needs board pass | **aligned** | arbor-growth | **Aligned — the cleanest of the ten.** A parent/teacher log of how a *specific situation* (drop-off, activity switch) went — "settled after a reminder / got stuck this time" — is the CI-23 behavior-noun (never "separation anxiety"), serves order (names the situation, not the child) and competence (points at a shared strategy + the real classroom). No deficit-noun, no score. **No reframe needed** if it stays situation-behavior framing and routes to a shared strategy/the teacher, never a "transition deficit" trend. Guardrail (the whole feature): name the situation, never the condition; no count of "failed transitions" rendered as a verdict. |
| FDI-03 | **Adult Mediation Index ("% of time the child needs an adult beside them")** | product | **gated → OFF-RUBRIC TRAP as framed** | n/a | needs board pass — **I flag this as a likely clinical VETO surface, not merely gated**: a single quantified per-child dependence score *is* a per-child verdict (the board's standing veto trigger) + a normed-functioning metric (MDR Rule 11 device-adjacent) | **off-rubric** | arbor-growth | **Off-rubric trap — and the perverse-incentive crux of the whole proposal.** A headline number quantifying how dependent a child is fails the rubric on four lenses at once: **competence→dependence** (it trains parent + teacher to see the child as a dependence quotient, not a developing person — and the *child's* competence is literally re-encoded as a deficit %); **truth→avoidance-by-distortion** (a single % flattens enormous normal preschool variation into a rank, reads as precise when it is noise, and a 3yo needing an adult is *normal*, not a measurable impairment); **responsibility→reassurance/resource-seeking** ("are we above the aide threshold?"); **meaning→engagement** — and worse, a **perverse incentive**: the report's value to the family *rises as the child looks more impaired*, so the metric is optimized UPWARD. That is the inversion of every lens. **Reframe (the support-routing version):** drop the index. Keep the underlying signal as a **parent/teacher descriptive note of where [child] currently needs support** ("needs an adult alongside for the activity switch and the noisy mat-time"), surfaced as *where to lean in + what to ask the gan for*, never a quantified dependence score, never trended as a number. The honest, competence-building artifact is "here is where this child needs scaffolding, and here is who can provide it" — NOT "this child is 64% dependent." Even reframed it stays gated. **My voice = likely board VETO; flagging so the council does not mistake a reframe for clearance.** |
| FDI-04 | Social-Communication "areas of difficulty" read | product | **gated → OFF-RUBRIC TRAP as framed** | n/a | needs board pass — **likely VETO**: an SRS-derived "areas of difficulty" read is a per-child autism-adjacent screen (condition inference, MDR Rule 11; the exact v2 risk-map class the board vetoed) | **off-rubric** | arbor-ai | **Off-rubric trap — pathologizes by design.** "Areas of difficulty" derived from SRS items is a deficit-enumeration of a child's social communication = a soft autism screen by the back door. Fails **truth** (a deficit list reads as a verdict and pathologizes normal social variation) and **competence** (it trains the family to see a bundle of deficits to document). **Reframe:** convert to FDI-02's behavior-situation logging + the CI-23 *non-diagnostic referral rail* — "you/the teacher noted [child] finds back-and-forth play hard at the moment; that's worth raising with your pediatrician or an SLP" (reuse CI-25 string, never invent, never auto-fire). The honest move is *route to a real SLP*, never *issue Arbor's own social-comm deficit read*. **My voice = likely VETO on any SRS-mapped "difficulty read"** — clearable only as observation + the existing referral rail. |
| FDI-05 | Attention / Participation Profile | product | gated | n/a | needs board pass — **likely soundness:concerns**; ADHD-adjacent if rendered as a per-child attention score/profile (CI-22 vetoes EF/attention scores; no provider-routing on attention below the age-7 floor) | tension | arbor-growth | **Tension — observable participation is loggable; an "attention profile" is the CI-22 trap.** How a child participated in a *specific* gan activity ("joined circle / drifted after 5 min / needed a 1:1 prompt") is an observable behavior and can serve order. But a per-child *attention/participation profile-noun* is the EF/attention-score class CI-22 vetoes, and an attention "deficit" read is ADHD-adjacent. **Reframe:** log participation per activity as a parent/teacher observation routed to a shared strategy ("what helped [child] stay with circle time"), child-as-own-baseline, never an attention score/rank/profile, no provider-routing on attention alone below age 7. Climbs toward aligned as a log; vetoed as a profile. |
| FDI-06 | Sensory / Emotional trigger map | product | gated | n/a | needs board pass — **likely soundness:concerns(clearable)** as the CI-62 sensory *preference/comfort* log + the CI-24 fire-once emotion pattern; **VETO** if it becomes a Sensory-Profile screen or an emotion-intensity trend | tension | arbor-growth | **Tension — a "what helps [child]" comfort log is sound; a Sensory-Profile screen + emotion-intensity trend are vetoed.** Reuse the existing rails verbatim: CI-62 (a parent/teacher *preference/comfort* log read back as "what helps [child] feel comfortable" — never `sensory profile`/`sensory seeker`/`over-/under-responsive`, no count-of-signs→SPD threshold) + CI-24 (generic emotion check-in, intensity internal-only never charted, the fire-ONCE outward-routing escalation, ≥90d cool-down). **Reframe:** "triggers + what soothes" as a shared-strategy comfort map for parent AND teacher to act on, never a sensory/emotional *deficit* read. The teacher knowing "the hand-dryer sets [child] off, here's what helps" is genuinely competence/order-building. Held to the CI-24/CI-62 guardrails it clears; a Sensory Profile instrument is the VETO. |
| FDI-07 | **Committee-ready professional report (justify a סייעת / support hours to a ועדה)** | product | **gated → CEO-gated + the institutional-buyer crux** | n/a | needs board pass — **HELD, re-touches the #95/#97 clinician-export decision (CEO-gated at 12mo)**; a committee-facing "professional report" implies Arbor *produces/endorses* a clinical assessment (firewall §3 p11) + is the deepest third-party child-data egress (the deficit dossier leaves to an institution) → Guy + arbor-safety + clinical-lead, never auto-built | **tension** (genuine fork) | arbor-growth | **The hardest call — a genuine fork, not a clean trap.** The competence/responsibility-aligned reading is real and strong: a struggling child *getting the aide they actually need* via a real eligibility committee is the rubric working — competence (the child gets a real human + real resources), responsibility (the parent acts to secure support), order (turns scattered worry into the document the system requires), truth (names the hard reality to get help), and it routes OUTWARD to real institutional support, not deeper into the app. **But three inversions sit inside it.** (1) **Institutional-buyer capture:** if the ועדה/gan is the buyer and "report generated" is the monetized artifact, the product tilts toward *producing reports* over *helping children*, and toward the institution over the family — Hazard B. (2) **The perverse incentive (Hazard A, sharpest here):** a report whose *purpose* is to justify support has its value rise as the child looks more impaired → an engine that optimizes documented-deficit upward, which is care-substitution's opposite twin: not "the app reassures you nothing's wrong" but "the app maximizes what's wrong to win resources." Both distort truth. (3) **Firewall + claim:** a "professional/clinical report" from Arbor = a clinician-endorsement/assessment claim (§3 p11) the moment it reads as Arbor's professional judgment rather than the *family's own observations they carry in*. **Reframe (the only sound near-term form — reuse the DEM-005 / #95 / B3 stance verbatim):** a **parent-owned export of the family's and teacher's own observations** that the *family brings to their own provider / the committee* — Arbor structures and hands over *what the parents and teacher logged*, never Arbor's verdict, never a deficit score, never "Arbor finds [child] needs an aide." Success metric = *the child got the support a real committee decided they needed* and *a real professional made the call*, NOT reports-generated / committee-approvals / institutional seats sold. The export carries observations + the non-diagnostic referral rail, and explicitly states Arbor does not assess or diagnose. Built that way it is competence/responsibility-aligned and routes outward; built as "Arbor's committee report" it is the institutional-buyer + maximize-deficit inversion. **HELD — re-touches the CEO-gated clinician-export decision; my voice coincides with a clinical/safety VETO on any Arbor-authored professional/clinical verdict or third-party egress without Guy + arbor-safety + clinical-lead.** |
| FDI-08 | Intervention Planner | product | gated | n/a | needs board pass — **soundness:concerns**; "intervention" is a clinical-effect word (CI-23 bans therapy/intervention efficacy framing); clearable as a parent-guided shared-activity plan with no effect claim | tension | arbor-practice | **Tension — a shared-strategy plan is competence-positive; "intervention" + effect claims are the CI-23 trap.** Handing the parent/teacher concrete next steps tied to what they logged serves competence + order + meaning *if* the metric is the child + real-world action, not plan-completion. The hazards: the word "intervention" implies a clinical treatment loop, and any "this will reduce his meltdowns / improve regulation" is the effect-size claim ceiling the board vetoes. **Reframe:** an unbranded *parent/teacher shared-activity plan* ("try this together / suggest this to the gan"), mechanism-only, every step routes to a real shared activity or the teacher, never an "intervention that works," never a branded program (Zones/Hanen/etc. banned per CI-23). Success = a strategy the parent/teacher reports working + the child's own progression, never modules/plans completed. Clears as activities; vetoed as efficacy-claimed intervention. |
| FDI-09 | Before / After Progress view | product | gated | n/a | needs board pass — **soundness:concerns**; a before/after *on a deficit/mediation metric* is the intensity-trend-chart class CI-24 vetoes + an implicit efficacy claim ("Arbor reduced the deficit") | tension | arbor-growth | **Tension — child-on-own-curve progress is sound; before/after *on a deficit score* is the trap.** Reflecting a child's own progression over time is order/meaning-positive and the moat doing honest work — IF it trends *the parent's own "this worked" observations* and the child's own behaviors. The trap: a before/after delta on the Adult Mediation Index or a "difficulty" score is (a) an emotion/deficit-intensity trend chart (CI-24 veto), and (b) an implicit "Arbor improved my child" efficacy claim (the claim ceiling). **Reframe:** show the child's *own* observed-behavior progression ("more two-word phrases this week, per what you noted" — the CI-95 parent-attributed, non-metricized recall), never a deficit-delta, never "mediation down 12%," no effect-on-the-child verb. Clears as honest own-baseline recall; vetoed as a deficit-reduction chart. |
| FDI-10 | Parent–Teacher Shared View | product | gated | n/a | needs board pass + **arbor-safety**: a gan teacher writing to a child's record is a NEW third-party child-data write path (COPPA/GDPR; teacher = not the parent/guardian — consent + data-controller question) | **aligned** (reframed) | arbor-growth | **Aligned in substance — bringing the gan teacher onto the parent-owned record is the second-guardian loop (cf. DEM-007 reframe), and a shared parent–teacher picture is genuinely order + competence: the two adults around the child act on one view, the family stays the owner.** Two hard guardrails make or break it. (1) **The teacher must be a contributor of observations to the FAMILY's record, never a data-entry tier feeding a committee-report engine** (Hazard B) — if the teacher becomes free labor producing the monetized deficit dossier, it inverts. Frame the teacher's role as *helping this child*, the parent as owner, the shared view as a coordination surface, never a documentation pipeline. (2) **Consent/data path:** a non-guardian adult writing to a child's record is a real COPPA/GDPR controller-consent question — parent grants/governs/revokes the teacher's access (the PHI-08 ownership frame), arbor-safety clears the write path. Reframed to "two caregivers, one parent-owned record, in service of the child" it is aligned; left as "teacher fills in deficit fields for the report" it is the institutional-buyer trap. |

**Advisory net — the council reads weighting, the board reads veto:**
- **3 that, reframed correctly, best serve competence-before-dependence** (the child gets real support, the family + teacher become more capable, Arbor routes outward and steps back): **FDI-07 Committee Report** *as a parent-owned export of the family's/teacher's own observations brought to a real provider/committee* (the highest-stakes win — a struggling child gets a real aide via a real committee, Arbor structures and hands over, never verdicts); **FDI-02 Transition Tracker** (situation-behavior log → shared strategy + the real classroom, the cleanest as-framed); **FDI-10 Parent–Teacher Shared View** *as the second-guardian loop* (two caregivers on one parent-owned record, family stays owner). FDI-06 (comfort/trigger map held to CI-62/CI-24) is the strong runner-up.
- **2 traps to refuse as framed:** **FDI-03 Adult Mediation Index** (a quantified per-child dependence score = the perverse-incentive crux; off-rubric on four lenses + a per-child verdict the board vetoes — drop the index, keep a descriptive "where [child] needs support" note) and **FDI-04 Social-Communication "areas of difficulty" read** (an SRS-mapped deficit read = soft autism screen; off-rubric on truth + competence — replace with observation + the non-diagnostic referral rail). FDI-01's *instrument-mapping* is the third thing to refuse (clearable only as a descriptive log, never an ABAS/Vineland profile).
- **The crux, stated plainly for the council:** the same support-routing capability is competence/responsibility-aligned when it gets a real child real help via real humans and Arbor steps back — and is the inversion when it (a) quantifies the child as a deficit to optimize upward, (b) sells the report to the institution over the family, or (c) turns the teacher into a documentation tier. The dividing line on every item is *parent-owned observations routed outward to real support* (aligned) vs *Arbor-authored deficit verdict monetized to an institutional buyer* (trap + veto).
- **Veto-coincidence flags (so weighting is not mistaken for a green light):** FDI-03, FDI-04, and the instrument-mapping in FDI-01 — my advisory call coincides with a **likely clinical/safety VETO** (per-child verdict / condition-inference / MDR Rule 11). FDI-07 re-touches the **CEO-gated clinician-export decision** + deepest child-data egress + firewall §3 p11 → HELD, Guy + arbor-safety + clinical-lead, never auto-built. Every FDI item is `gated`; none is advisory-clearable to build-ready, and advisory weighting on the tension items is NOT board clearance.

## Clinical GATE — 2026-06-23 — CI-29 Activity Personalization (interests[] + interest-boost selector) → soundness:pass(conditional) / claims:none / riskClass:gated → BUILD-READY-NARROWED

**arbor-clinical-lead** + peds/slp/psych ran the binding gate on **CI-29** (AP-073 candidate): an `interests[]` field on ChildProfile collected in ProfileEditDrawer, a deterministic interest-boost multiplier in `rankDailyPlay` (LLM-free default path), and an interest-match Chip + why-line on DailyPlayCard. The LLM theme-rewrite is correctly fenced out of this build (gated on arbor-ai cost review, CIL para 2). Reuses the binding bar in `mesh/improvement/CLINICAL-GUARDRAILS-CI-22-23-24.md` + the CI-62/CI-33 preference-log precedent + the 100-Capability Blueprint #2 output-floor rule — **not re-derived.**

**Verdict: soundness:pass(conditional) / claims:none / riskClass:gated → BUILD-READY-NARROWED.** No clinical VETO. CI-29 is the safest class of personalization the board reviews: an **interest is a parent-logged preference, not a developmental signal** — the CI-62/CI-33 comfort-log pattern (child-as-own-baseline, descriptive, zero interpretation), closer to "favorite color" than to any milestone/competency/domain. There is **no per-child score/verdict/risk-map, no SRS/Vineland/ABAS construct-clone, no autism/ADHD inference, no effect-size claim** in the design as written. The interest-boost is a *relevance re-rank*, not a developmental claim — the spec is explicit that whatItBuilds/items/steps/developmental content are byte-identical to today. It clears as written **provided the five narrowing fixes below are in the actual strings/code**; `riskClass:gated` stands solely on the COPPA child-data write path (Guy + arbor-safety), not on a clinical hold.

**Required fixes (binding before build-ready — narrowing, not blocking):**
1. **Interest record stays a record, never an interpretation (the one real latent hazard — peds + psych co-flag).** The longitudinal "trains at 2 → dinosaurs at 4" framing is safe as a *preference history*; it must **never** be read back as a developmental signal. **Hard-banned in any string, label, tooltip, empty-state, or pattern view:** `restricted interests`, `restricted and repetitive`, `repetitive interests`, `fixation`, `fixated`, `obsession`/`obsessive`, `perseveration`/`perseverative`, `narrow interests`, `intense interest` as a flag, `special interest` as a clinical noun, `hyperfocus`. These are the autism-adjacent condition-inference class (FDI-04 / CI-24 veto). No count, trend, breadth-score, or "interests narrowing/broadening over time" verdict. The record is displayed; it is never *interpreted*.
2. **No effect-verb leak into the why-line / interest-match copy (claim ceiling).** The why-line ("Chosen because [name] loves [interest] — and it matches what you have been working on / fits this stage") is clean as a **relevance/mechanism** statement and may ship. Hold the firewall: none of `improves`/`builds`/`boosts`/`trains`/`strengthens`/`develops`/`reduces`/`accelerates` may attach to the child's capacity via the interest framing ("trains-themed activities build his focus faster" = blocked). Mechanism only: the interest *frames* the activity, it does not *improve the child*.
3. **Sanitize the free-text interest before it echoes into card copy (copy-injection floor).** The free-text "Add" input is parent-typed and is rendered verbatim into "Matched to [name]'s love of [interest]." Run the interest token through the existing banned-string/`CONDITIONS` lexicon (`safety/outputScreen.ts` regex) **at render into copy**, so a parent-typed condition word ("autism," "speech delay," "ADHD," "anxiety") can never render as "[name]'s love of [autism]" in a card. Drop/neutralize a flagged token from the boost-copy path (it may still persist as a raw note if safety permits, but never surface in a card sentence). This is a sanitize/lint requirement, not model-screening — no LLM authors this string in Phase 1.
4. **LLM theme-rewrite stays a SEPARATE gate.** When the guarded follow-up ships, every model-rewritten activity/story/why string passes `screenModelOutput` with the capability's `prohibitedDiagnosticClaims` compiled into `CONDITIONS` + a passing output-screen test (the 100-Capability Blueprint #2 bar, made load-bearing). Not cleared by this run; route as its own clinical + cost gate.
5. **AAC / minimally-verbal non-exclusion + parent-mediated (slp + psych).** Interest entry is **parent-entered**, so a minimally-verbal or late-talking child is not excluded or implicitly flagged — confirm the collection point is the parent surface only (it is: ProfileEditDrawer, no child-facing entry). The interest framing must not drift into a kid-companion ("Arbor knows you love trains!") — the card speaks to the parent about the child, never to the child as a companion.

**Lens splits:** **peds — pass, owns fix #1** (an interest *record* carries no milestone/red-flag interpretation; the only block is letting interest-drift read as a restricted-interest marker). **slp — pass, owns fix #5** (an interest is not a communication/language signal and must never be counted as one; parent-entered = no expressive-language gate, no minimally-verbal exclusion). **psych — pass, owns the framing** (interest-as-identity is attachment-positive and competence-building — it hands the parent back *who their child is*, routed through the parent at a shared activity; holds fixes #1/#2/#5). All three: pass, conditional on the narrowing.

**Procedural gate (independent of clinical):** `interests[]` is a NEW child-data write path → **arbor-safety COPPA/GDPR review** on the write path + Guy sign-off before build-ready (riskClass:gated stands on this alone). Firewall PASS — all CI-29 copy is "developmentally informed," carries no "clinically validated"/"clinician-reviewed," no adviser/thinker identity; hold it through the build.

**cited_basis:** CI-62/CI-33 preference-log precedent (parent-logged comfort/preference = descriptive log, zero interpretation, child-as-own-baseline); CDC LTSAE 2022 + AAP surveillance (an interest is not a milestone or a marker — restricted/repetitive-interest reads belong to a qualified professional's evaluation, never an app's preference field); ASHA scope (interest ≠ communication signal); 100-Capability Blueprint #2 (output-floor for the future LLM-rewrite); CHARTER §3 p11 / advisory.md §0 (firewall, no clinician-endorsement).

**Verdict:** soundness:pass(conditional) / claims:none / riskClass:gated → **BUILD-READY-NARROWED.** Buildable with the five narrowing fixes in the actual strings/code; not auto-built (gated on arbor-safety COPPA + Guy). The LLM theme-rewrite is fenced to its own future gate. Routed through `arbor-orchestrator`.

## Clinical sign-off — 2026-06-23 — binding copy/framing gate on committed gated items

**Board:** arbor-clinical-lead (+ arbor-clinical-psych lens). **Mode:** review-only, no code edits. **Worktree:** `.arbor-redesign-w4` (branch `claude/redesign-wave4-gated`), commits `edf82b6` (AP-049 Step 3) + `43fb29c` (AP-057). Checked against the cleared copy in `PAI/PRDs/arbor-redesign-handoff/_analysis/GATED-CLEARANCES-CLINICAL.md`.

| Item | soundness | claims | riskClass | Verdict |
|------|-----------|--------|-----------|---------|
| AP-049 Step 3 — focus-domain picker | pass | none | gated | **PASS** — Step-3 strings (title/subtitle/multiHint/skip/footer) match cleared copy VERBATIM in `i18n.ts` EN + HE; 7 tiles = name + warm capability one-liner, no deficit; render in `OnboardingFlow.tsx` shows no score/status/low-high/needs-work/red styling (selected = green-soft + checkmark only). HE transcreated, non-grading meaning preserved. |
| AP-057 — Bedtime Stories framing | pass | none | gated | **PASS** — `bedtimeStories.ts` framing contract is warm, strengths-based, parent-mediated, non-diagnostic; explicitly bans "delay"/"behavior problem"/"meltdown"; `NON_DIAGNOSTIC_CONTRACT` prepended verbatim in `/generate-bedtime-story` (`api.ts`), with escalation-screen + redaction + generate-and-discard seams wired. |

`required_fixes: none (clinical copy/framing gate CLEARED)`
`cited_basis: [CDC LTSAE 2022 individual-timeline · AAP strengths-based/non-labeling · Siegel & Bryson co-regulation/affect-labelling]`

**Still blocked (clinical does NOT lift these):** AP-049 + AP-057 remain Tier-C and still carry the OPEN **arbor-safety COPPA** gate (Step-4 avatar; child-day input) per the clearance doc + Guy individual sign-off. Clinical PASS clears only the copy/framing gate; neither is build-ready until arbor-safety clears and Guy confirms. Route through arbor-orchestrator.

---

## 6. Rules
1. **One backlog.** The council is the *only* legitimate birthplace of a build item. Pods and loops draw from `PRODUCT-BACKLOG.md`; they do not invent scope.
2. **Propose, don't clobber.** The loop appends a dated candidate block; it never edits the canonical backlog body. Promotion is a deliberate step.
3. **The clinical gate is binding.** No claim-bearing item is build-ready without Clinical Board sign-off.
4. **Advisory is weighting, not gating.** The rubric shifts priority; it never blocks.
5. **Gated never auto-builds.** `gated` and `[FOUNDER]` items are surfaced for Guy, not scheduled.
6. **Every cycle ends in memory** — append a one-liner to [MEMORY.md](MEMORY.md) and `/PAI/MEMORY.md`: streams pulled, candidates written, top build-ready, gated decisions surfaced.

## 7. Related
- [teams/advisory.md](teams/advisory.md) · [marketing/MESH.md](marketing/MESH.md) · [improvement/CIL.md](improvement/CIL.md) · [ORCHESTRATOR.md](ORCHESTRATOR.md) · [CHARTER.md](CHARTER.md) §3 principle 7
- Canonical backlog: [../PRODUCT-BACKLOG.md](../PRODUCT-BACKLOG.md)
- Workflow: `/.claude/workflows/arbor-product-council.workflow.js` · Registry row: [/00_System/agent-framework/SCHEDULED-LOOPS.md](../../../../00_System/agent-framework/SCHEDULED-LOOPS.md)
